---
name: user-profile
description: 独立游戏开发者，中文项目「破碎之城」类银河恶魔城×暗黑ARPG，数据驱动CSV工作流，偏好精确格式规格
metadata:
  type: user
---

用户是独立游戏开发者，项目「破碎之城」为 Unity 2D 类银河恶魔城 × 暗黑 ARPG，中文工作语言，引擎 Unity 6000.4.8f1。

**Why:** 用户给出非常精确、机器可校验的数值规格（CSV 列格式、min:max:pct 段语法、白名单属性、保底候选数约束），并要求完成后用脚本验证。说明用户重视数据可验证性和一致性。

**How to apply:**
- 输出中文数值表/设计文档时严格遵循用户给出的格式规格，不要自行改动列结构或字段语义。
- 数值必须可校验（min≤max、白名单属性、档位候选≥2 等），交付前自查。
- 尊重 balance-design.md 已批准数值作为基准，改动需显式标注。
- 用户角色分工明确：经济策划产出数据表，不写实现代码；跨系统改动需标注并交回（见 [[project-affix-redesign]]）。
