# 🎮 《类银河恶魔城》游戏系统深度解剖报告

> 制作视频讲解宣传用 — 全面拆解各系统模块

---

## 📋 项目概览

**引擎版本**：Unity 2D  
**游戏类型**：Metroidvania（类银河恶魔城）+ RPG 元素  
**核心特色**：丰富的技能组合系统、元素战斗、装备系统、任务系统、网络联机支持  
**当前状态**：开发中（功能框架基本完成）

---

## 一、🎯 核心架构 — Entity + StateMachine

### 1.1 实体基类体系 (`Character/Entity/`)

```
Entity (基类) ─┬─ Player (玩家)
               └─ Enemy (敌人)
```

**Entity 基类** 是所有角色的根基，提供：
- **物理碰撞**：地面检测（`groundCheck`）、墙壁检测（双点检测）
- **状态机托管**：持有 `StateMachine` 实例，每帧调用 `UpdateActiveState()`
- **击退系统**：`ReciveKnockback()` 协程控制受击后退
- **顿帧系统**：`StartHitStop()` / `EndHitStop()` 控制受击冻结
- **减速系统**：`SlowDownEntity()` 协程控制
- **翻转系统**：`Flip()` + `OnFlipped` 事件（连带血条翻转）

### 1.2 状态机 (`StateMachine.cs`)

**工作原理**：`Initialize(初始状态)` → `ChangeState(新状态)` 退出旧+进入新 → `UpdateActiveState()` 每帧刷新

**状态基类体系**：`EntityState` → `PlayerState`（含输入处理/土狼时间/跳跃缓冲）| `EnemyState`（含朝向/动画触发）

**玩家状态（12种）**：

| 状态 | 功能 |
|------|------|
| IdleState | 待机 |
| MoveState | 移动 |
| JumpState | 跳跃（土狼时间） |
| DoubleJumpState | 二段跳（技能解锁） |
| FallState | 下落（动态重力） |
| WallSlideState | 爬墙滑行 |
| WallJumpState | 蹬墙跳 |
| DashState | 冲刺（技能解锁） |
| BasicAttackState | 普通攻击（4段连击） |
| JumpAttackState | 跳跃攻击 |
| CounterAttackState | 反击 |
| DomainExpansionState | 领域展开 |
| CounterChaseState | 反击追击 |
| DeadState | 死亡 |

**敌人状态（7种）**：Idle → Move → Battle → Attack → Stunned → Dead

### 1.3 动态重力系统

不同于常规的恒定重力，精细控制：上升重力缩放 (0.9x) vs 下落重力缩放 (1.3x)，二段跳独立参数

### 1.4 跳跃手感优化

- **土狼时间**：离开平台后 0.1s 仍可跳跃
- **跳跃缓冲**：提前按键有效
- **跳跃形变**：压扁→拉伸动画

---

## 二、⚔️ 战斗系统

### 2.1 攻击系统

**连击机制**：4段基础连击，超时重置
**特殊攻击类型**：跳跃攻击(1.2x)、追击攻击(1.2x)、第三段连击(1.2x)，各有暴击加成

**伤害公式**：
```
最终伤害 = (物理伤害 + 元素伤害) × 技能倍率 × 暴击倍率
```
- 物理 = 属性 + 力量×0.5
- 元素 = 主元素 + 次元素 + 智力加成 + 元素之心加成
- 暴击率 = 基础暴击 + 敏捷×0.5

### 2.2 反击系统

**流程**：敌人攻击窗口 → 玩家反击 → 关闭窗口 → 震屏+粒子+音效 → 顿帧 → 可追击 → 激活自动追击

### 2.3 顿帧系统 (HitStop) — 三段式设计

```
冻结阶段 (50%时间) → 慢恢复 (30%时间) → 快恢复 (20%时间)
动画速度=0              曲线0→0.3          曲线0.3→1
```

- **全局顿帧**：修改 Time.timeScale
- **局部顿帧**：仅冻结目标+攻击者，AnimationCurve 恢复

### 2.4 元素状态效果

| 元素 | 效果 | 机制 |
|------|------|------|
| 🔥 火焰 | 持续灼烧 | 每秒2跳，总伤害固定 |
| ❄️ 冰霜 | 减速 | 倍率+时长受冰抗影响 |
| ⚡ 闪电 | 蓄能爆发 | 累计电荷达阈值落雷 |

### 2.5 护甲与闪避

护甲减伤：`有效护甲/(有效护甲+100)`，上限75%；闪避上限75%

---

## 三、🧙 技能系统

### 3.1 技能架构

`Skill_Base` 基类 → 10+ 技能：冲刺/残影/碎片/时间回响/领域展开/火/冰/雷/二段跳/元素精通/强力追击
- 冷却管理、等级系统、CD缩减
- 技能数据由 `ScriptableObject` 配置

### 3.2 升级分支

**冲刺系**：残影→存储→残像→碎片
**碎片系**：追踪→多重充能(3层)→传送→血量回溯
**时间回响系**：单次→3连击→复制→治疗精灵→净化→CD缩减
**领域展开系**：减速领域→回响领域→碎片领域

### 3.3 技能槽位

5个槽位(H/Y/U/I/O)，拖拽绑定/解绑/交换，被动技能解锁即生效

---

## 四、📦 物品与装备系统

### 4.1 背包系统
字典存储、堆叠管理、移动/交换物品、更新事件

### 4.2 战利品系统
多层级掉落：LootPool/LootTable → 稀有度(普通→传说) → 物理抛物线掉落(圆形散布)

### 4.3 装备系统
多类型多槽位，装备属性加成自动应用到角色属性，支持交换/重新计算

---

## 五、📊 属性系统

**四大属性组**：资源组(HP/恢复) | 主属性(力量/敏捷/智力/活力) | 攻击组(物伤/暴击/元素) | 防御组(护甲/闪避/抗性)

| 主属性 | 影响 |
|--------|------|
| 力量 | 物伤+0.5/点、暴伤+1/点 |
| 敏捷 | 暴击+0.5/点、闪避+0.5/点 |
| 智力 | 元素伤害+0.25/点、抗性+0.25/点 |
| 活力 | HP+5/点、护甲+0.5/点 |

**元素之心**：同时影响HP、元素伤害、无元素攻击保护

---

## 六、⬆️ 经验与等级系统

升级获得属性点+技能点，分段成长（1/2/3 点递减）。0-5级自动加主属性（新手保护）

---

## 七、🧟 敌人系统

**三种类型**：普通/精英/Boss，各有独立倍率配置（体型/属性/速度/范围）
**AI行为**：巡逻→追击→攻击→退避→眩晕
**等级系统**：敌人可配置等级，按级提升属性

---

## 八、🗺️ 区域系统

玩家进入 → 生成敌人波次 → 支持重生 → 区域难度 → 清空事件通知
首次进入显示遭遇提示，离开冷却后重生

---

## 九、📜 任务系统

**生命周期**：可接→已接受→可领奖→已完成 | 失败

目标类型：击杀/收集/探索
特色：前置任务链、接受时同步背包、提交扣物、追踪系统、事件提示

---

## 十、🎨 视觉特效

受击闪烁/伤害弹出文字/元素状态闪烁/暴击粒子/屏幕震动/反击冲击波
预制体：受击/暴击/落雷/碎片爆炸/烟雾/残影
视差背景多层滚动

---

## 十一、🔊 音频系统

BGM淡入 | 环境 | UI | 角色(脚步/跳跃/落地) | 战斗(挥砍/命中/暴击/反击)
三路音量独立控制，PlayerPrefs持久化

---

## 十二、🖥️ UI 系统

快捷键面板切换(Tab/Key/Esc) | 角色面板 | 技能树面板(节点/连线/前置/冲突/拖拽) | 背包(拖拽/高亮/装备) | HUD(血条/经验/任务追踪) | 提示系统(事件/技能/物品/属性) | 聊天系统

---

## 十三、🌐 网络系统

基于 LiteNetLib，支持 Host/Client/Single 模式
抽象传输层 `IGameTransport`，可靠消息通道(序列号+ACK+超时重传)
NetworkObject + NetworkBehaviour + SpawnSystem

---

## 十四、🛠️ 编辑器工具

CSV批量导入：物品/战利品表/任务/属性配置
装备系统可视化编辑器、背包编辑器

---

## 十五、💡 其他亮点

输入缓冲、存档框架、宝箱/Buff/垂直平台交互、自定义Shader

---

## 🎬 视频讲解建议

**推荐章节**：
1. 开场 — 游戏类型+视频大纲
2. **核心战斗** — 连击/反击/元素/顿帧
3. **技能系统** — 技能树/领域展开/时间回响/碎片（最炫酷）
4. **装备与物品** — 背包/稀有度/装备搭配
5. **角色成长** — 属性分配/升级
6. **敌人与区域** — 类型/难度
7. **任务驱动** — 引导流程
8. **UI与体验** — 面板/操作
9. **网络联机** — 多人模式
10. **结尾** — 路线图+期待关注

**重点演示**：领域展开大招、碎片传送+血量回溯、反击追击连段、元素战斗特效、视差背景、CSV数据驱动
