# 《破碎之城》Demo 演示 + 技术力讲解方案

> 面向岗位：Unity 客户端 / 游戏开发实习
> 用途：① 录制功能展示视频 ② 面试技术自述与答辩
> 代码基线：git HEAD（master，92 commits）
> 本文所有结论均来自对源码、GDD、审查报告的实际核查，标注了文件路径可现场指认

---

## 第 0 部分：先用 30 秒建立认知（视频开场词）

> "这是一个 2D 类银河恶魔城 + RPG 的 Demo，名叫《破碎之城》。
> 全部程序由我一人完成，**274 个 C# 文件、约 26,000 行自研代码**（准确值 25,983 行），另有 2,100 行编辑器工具。
> 引擎是 **Unity 6000.4.8f1 + URP**，核心架构是 **组件式有限状态机 + 事件驱动 + ScriptableObject 数据驱动**。
> 今天我按'手感 → 战斗 → 技能 → 成长 → 内容管线'的顺序展示主要系统。"

**为什么要先报数字**：面试官看 Demo 视频时，10 秒内要判断"这是不是抄的教程项目"。给出体量和引擎版本，等于先立起"独立完成 + 中大型项目"的锚点。

---

## 第 1 部分：演示内容取舍（最重要的一节）

你的原话是"不需要展示全流程，只用展示主要功能系统"。但更关键的判断是**哪些不能展示**。

我把项目里的系统按"能不能撑住 30 秒不露怯"分了三档：

### ✅ A 档 — 必须展示（技术密度高、演示效果好、实现完整）

| 系统 | 演示时长 | 为什么它值得占时长 |
|------|---------|------------------|
| 玩家移动手感（状态机） | 60s | 14 状态 FSM、土狼时间、5 路输入缓冲、动态重力、跳跃压扁拉伸——**这是面试官唯一能"用手感感知"的技术** |
| 战斗：3 连击 + 反击 + 追击 | 90s | 反击→追击是原创连段循环，含无敌帧接管、顿帧开关、追击眩晕，逻辑复杂度远高于普通教程 |
| 顿帧系统（HitStop） | 30s | 全局 timeScale + 局部逐目标 AnimationCurve 抬升，双轨设计，是"知道怎么做手感"的证明 |
| 技能系统 + 技能树 UI | 120s | 35 个技能数据资产、14 个技能实现、5 槽位拖拽、升级分支；**领域展开 + 时间回响 + 碎片三者互相调用**是最大的技术亮点 |
| 元素状态（火/冰/雷） | 40s | 灼烧 DoT / 减速 / 电荷累计落雷，三种完全不同的状态机制 |
| 属性系统 + 装备词缀 | 60s | Stat 的双乘区 Modifier 链（`source` key 精准回退）+ 程序化词缀生成（池化 / Tier 保底 / **稀有度调性表**） |
| 精英词缀（16 个） | 45s | `IEnemyAffix` 生命周期接口 + 泛型加权选择器 + **运行时 `AddComponent` 注入**，16 个词缀覆盖光环/新星/分身/自爆/护盾/吸血 |
| 铁匠三件套（制作/分解/合成） | 50s | **一张配方表双向服务制作与分解**；合成把内部概率全部暴露给玩家（透明化随机） |
| Boss 战（史莱姆王） | 90s | 4 阶段状态机 + 跨阈值分裂 + 濒死保命 + 动作池冷却调度器，**全项目 AI 复杂度顶点** |
| 任务树 GraphView 编辑器 | 60s | 自研 Unity GraphView 编辑器窗口（1118 行），**这是最能证明"不止会写玩法代码"的一项** |
| **存档往返（限定版）** | 60s | **第二强差异化点**。`JsonUtility` 不能序列化 `ItemDataSo` 引用 → 用 `itemId` 字符串存 + `ItemLookup` 从 Resources 重建；恢复顺序有真实依赖（技能→背包，因为被动扩容决定容量）。**限定演示范围**见下方说明 |
| 技能树门控（能力解锁） | 40s | 用"点亮二段跳前后过同一段地形"证明银河城核心循环。这是门控的**真实实现** |
| CSV → ScriptableObject 数据管线 | 30s | 策划改表 → 一键导入 → 114 个 SO 资产，**证明工程化意识** |

### ⚠️ B 档 — 可以提，但不要现场操作（有已知缺口，容易翻车）

| 系统 | 风险 |
|------|------|
| 存档系统 | **技术上扎实（见 A-9），是第二强差异化点，值得演示**——但必须限定范围。**可以演示**：装备词缀/稀有度/槽位、技能等级与槽位绑定、任务阶段与进度的还原，以及 pretty-print JSON 的可读性。**绝对不要演示**：读档后的 Boss 状态、传送门解锁状态（都不持久化） |
| 背包/仓库/商店/制作/分解/合成 | 功能齐全，但有**可当场复现的 bug**，必须先修（见下方"录制前必修的 6 个 bug"） |
| 区域/刷怪/传送门 | `Portal.cs` 单体可用，`AreaDifficulty` 表驱动质量最好；但 `PortalManager` 未实现，且区域事件全库无外部订阅者（只驱动提示文字） |
| 音频系统 | 三路音量 + PlayerPrefs 持久化，实现完整，但**占视频时长不划算**，一句话带过即可 |

### ❌ C 档 — 建议不展示（未实现或与核心玩法零接线）

这一档是关键，**因为你很久没碰项目，很可能记成"已经做完了"**。我逐个核实过：

| 系统 | 核实结论 | 证据 |
|------|---------|------|
| **能力门控 AbilityGate（作为独立类）** | ❌ **类不存在**，但**能力门控这件事是实现了的**——走的是技能树前置 | 全库 grep `AbilityGate`/`PortalManager` 零命中；目录 `AbilityGate\`/`PortalSystem\` 为空。**真正的门控是 `Skill_DataSo.preSkillRequirements`（`Skill_DataSo.cs:48`）+ `UI_TreeNode` 三重解锁校验 + `SkillDataManager.IsSkillUnlocked` + 递归锁子树**。见下方"门控的正确讲法" |
| **传送门网络 PortalManager** | ❌ **未实现**（仅单点传送 `Portal.cs` 可用） | 同上，零命中；Boss 场景靠 `BossEncounter.cs` 硬接线 `arrivalPortal.SetLocked(true)` / `exitPortal.SetActive(true)`，且不存档 |
| **Unity Input System / Action Maps** | ❌ **代码里完全没用** | `GameInput.cs` 是 `static class` + **旧版 `Input.GetKeyDown(KeyCode)`**（L71/76/81）+ 自研 `Dictionary<Action, KeyCode>` 表（L114-141）+ `PlayerPrefs` 持久化。全项目 **零处** `using UnityEngine.InputSystem`；`Assets\PlayerInpuSet.inputactions` 只有 **73 字节**（maps 为空、无人引用）。包在 manifest 里装着，但代码没接 |
| **"任务完成改变世界"（WorldState）** | ❌ **WorldState 是只写不读的死状态** | 全库仅 3 处：`QuestManager.cs:356` 写入、`SaveManager.cs:274/517` 存/读。**`WorldState.Get()` 无任何调用者** |
| **任务触发器（拾取触发/自动接取）** | ❌ **未实现**。字段定义了但无消费方 | `QuestData.cs:27-28,100-101` 定义了 `AutoUnlock`/`ItemPickup`/`triggerNpcId`/`triggerItemId`，但运行时无任何读取；`MarkQuestFailed` 无调用者（失败态无法由玩法进入） |
| **网络联机** | ❌ **与核心玩法零接线**。全项目 `Networking`/`LiteNetLib` 之外的代码**没有任何一处引用** `NetworkManager`/`NetworkObject`/`NetworkBehaviour`/`SpawnSystem` | 我做了全量 grep，跨模块引用数为 **0** |
| **宝石镶嵌 F12 / 世界条件 P1** | ❌ 未实现 | `production/review/SUMMARY.md` 第 84 行 |

#### 🔴 录制前必修的 6 个 bug（成本低、收益高，否则演示会当场露）

这 6 条都是**在演示操作路径上会被直接看到**的问题，修起来都是几行代码：

| # | Bug | 位置 | 演示时会怎样 | 修法 |
|---|-----|------|-------------|------|
| 1 | **买不到东西时双重退款 → 可无限刷钱** | `ShopSystem.TryBuy` | 背包放不下时点购买，金币不减反增，被问"你这个经济系统..."很难答 | 退款逻辑去重，只退一次；`npcStocks` 同步不扣 |
| 2 | **商店买光一件后，后面商品点不动** | `UI_ShopPanel` 用 `npcSlots.IndexOf(slot)` 当商品下标 | 买完第一件，点第二件没反应——**观众会以为是游戏坏了** | 商品槽持有稳定的商品索引，不用 `IndexOf` |
| 3 | **分解后按钮仍可点、文案不刷新** | `UI_DismantlePanel.OnClickDismantle` 先置 `selectedItem = null` 再 `RefreshDetail()`（内部首行 null 早退） | 分解完界面卡住不刷新 | 调整调用顺序：先刷新再清空 |
| 4 | **未解锁的技能树连线不可见** | `UI_TreeConnectHandler.Awake` 中 `connectionImage` 必为 null → `originalColor` 恒全透明 | **技能树是重点演示内容**，连线看不到等于废掉一半展示 | 修正组件获取顺序/判空 |
| 5 | **HUD 可能永久不更新** | `UI_InGame` 在 `Start` 订阅但 `OnDisable` 退订、`OnEnable` 不重订 | "主菜单 → 游戏"这条最常见路径下血条/经验条不动 | `OnEnable` 里补订阅（或改在 `OnEnable`/`OnDisable` 对称） |
| 6 | **血条每帧全局查找 + 空引用缺口** | `HealthBar.Update` 每帧 `FindAnyObjectByType<Player>()`，`FindPlayerHealth` 缺 null 检查 | 偶发 NRE，且是明显的性能反例 | 缓存玩家引用 + 补判空 |

> **另外两条建议顺手修的**（不一定会被看到，但成本极低）：
> - `UI_PlayerStats.OnDestroy` 三个订阅只退订了一个 → 单例长期持有已销毁组件的委托
> - `GameInput` 默认键位表自带冲突：`MoveDown = S` 与 `PlatformDrop = S` 重复（`O` 键的 `SkillSlot5`/`DomainExpansion` 冲突已知）
>
> **为什么值得花这 1–2 小时**：第 4 条直接影响技能树（你的重点演示内容）的观感，第 1、2 条是"经济系统"这类面试高频追问点的硬伤。**主动修掉并说"这是我审查时发现的，已修复"比留着被翻出来强得多。**

#### ⚠️ 一个必须先解决的问题：任务内容只有 1 个

这是**唯一一个会让你最强卖点失效的内容缺口**，必须处理：

| 事实 | 数据 |
|------|------|
| 全库 `QuestData` 资产总数 | **1 个** —— `Assets\Resources\Data\新任务.asset`（questId 700001《保卫村庄》，2 阶段：杀骷髅×1 → 交血瓶×1） |
| `Assets\Resources\CSV\Quests.csv` 里的任务 | 2 行（310001《击杀骷髅》、310002《上交血瓶》），**没有对应的 SO 资产**，是孤立数据 |
| `Entities.csv` | 仅 6 行（角色 / 骷髅 / 链锤骷髅 / 村长 / 铁匠 / 医生） |

> **行动项（强烈建议在录制前做）**：花 30–60 分钟，用任务树编辑器补 4–6 个任务资产，并连成**一条前置链**（A 完成 → 解锁 B，B 与 C 互斥）。
>
> **为什么必须做**：任务树编辑器是你整个视频里技术含金量最高的单项。但如果画板上只有 1 个孤立节点，GraphView 的"节点 + 连线 + 前置校验 + 冲突高亮"全部展示不出来——观众只会看到一个孤零零的方块。**补 5 个任务资产的成本极低（纯编辑器操作，不写代码），但能把最强卖点从"看不出所以然"变成"一眼就懂"。** 这是整个演示准备里投入产出比最高的一件事。

#### ✅ 门控的正确讲法（这里有个反转，很重要）

我一开始把"能力门控"归进了不能展示的一档。**这是错的**——`AbilityGate` 作为一个独立类确实不存在，但**"用能力解锁来门控探索"这件事你是真的实现了的**，只不过承载它的是**技能树**而不是一个专门的 Gate 组件：

```
Skill_DataSo.preSkillRequirements[]        （Skill_DataSo.cs:48，含 preSkillType + preSkillLevel）
  → UI_TreeNode.CanBeUnLocked()            （三重校验：技能点够 / 冲突节点未解锁 / 所有前置满足）
  → UI_TreeNode.CheckAllPreSkillRequirements()  （跨技能树按类型全局查找前置节点）
  → UI_TreeNode.LockConflictNodes()        （递归锁定冲突分支的整棵子树）
  → SkillDataManager.IsSkillUnlocked()     （运行时查询）
  → 消费方：Player_AiredState.cs:31 等     （没解锁二段跳就真的跳不上去）
```

而且**解锁会持久化**（`SkillSaveData.learned` + 读档 `ApplySkillData` 重建）。

> **面试正确说法（这个说法比原来那个更强）**：
> "银河城的核心是'获得能力 → 打开新区域'。我没有单独写一个 `AbilityGate` 组件，而是把门控收敛到**技能树的前置依赖**上——`preSkillRequirements` 支持跨技能树指定前置技能及其最低等级，解锁校验有技能点、冲突分支、前置链三重条件，冲突节点会递归锁定整棵子树。二段跳、冲刺这些移动能力是真占技能点的，没解锁就过不去地形。这样门控只有一份真源，不需要在场景里逐个摆 Gate 组件、也不会出现'技能树点了但场景门没开'的不一致。"
>
> 这个回答体现的是**设计判断力**——"我不需要那个组件，因为已有机制能承载"比"我写了那个组件"更难做到。**加分。**
>
> **演示方式**：录一段"技能树点亮二段跳之前 vs 之后"的同一段地形对比。这比任何文字都有说服力。

**唯一要避免的**：不要说"我实现了 `AbilityGate` / 能力门数据系统 / 门控可视化标记"——那个类不存在，GDD `ability-gate.md` 也还停在 `In Design`。

> ⚠️ **这是本文档最重要的提醒**。你记忆中的"有限状态机"是准确的，而且比你以为的更强（不止一套，有玩家、敌人、Boss 三套）。
>
> 但下面这几件你可能会顺手提的东西，**当前代码里不存在可运行形态**：独立的能力门组件（`AbilityGate`）、传送门网络、网络联机接入玩法、任务自动/拾取触发、`WorldState` 驱动世界变化、Unity Input System。视频里一旦展示不出效果，反而暴露"不清楚自己项目状态"，这在实习面试里是硬伤。
>
> **正确做法**：能力门改用"技能树前置"这个真实实现来讲（见上，说法反而更强）；其余放进"技术储备 / 后续规划"，或者干脆不提。

### 关于网络系统的一个诚实建议

`LiteNetLib` 目录里 `Connection/`、`Dispatch/`、`Protocol/`、`Reliability/` 是**第三方库**，不是你写的。你在它之上写了 `IGameTransport` 抽象、`NetworkManager`、`NetworkObject`/`SpawnSystem`、`ReliableChannel`。如果面试官问起，**必须主动说清哪部分是自己写的**——把第三方库说成自研是致命失误。建议视频里完全不提网络，面试被问到时用"我做了传输层抽象和可靠性通道，但尚未接入玩法，是技术预研"来回答。

---

## 第 2 部分：视频分镜脚本（建议 9 分钟版）

### 剪辑节奏原则

- **每个系统 3 段式**：① 纯玩法画面（让画面说话，不配旁白或只配 BGM）→ ② 切到 Unity 编辑器（Inspector 参数 / Scene 视图 Gizmos）→ ③ 回到玩法验证参数改动效果
- **第 ② 步是拉开差距的关键**。90% 的 Demo 视频只放游戏画面。你切进编辑器展示 Inspector 上的参数块、Scene 视图里的检测范围 Gizmo、Profiler 曲线，面试官会立刻判断"这人懂工程，不只是会拼教程"。
- **不要加速播放战斗**，但**必须加速重复跑图**。

### 分镜表

| # | 时间 | 画面内容 | 旁白要点（技术力句） |
|---|------|---------|-------------------|
| 0 | 0:00–0:20 | 主菜单 → 标题 → Logo | 报体量数字 + 引擎版本 + 一句话定位 |
| 1 | 0:20–0:50 | **移动手感蒙太奇**：跑动→跳跃→二段跳→蹬墙滑行→冲刺，中间穿插一次"跳崖边缘起跳"（展示土狼时间） | "14 状态 FSM。土狼时间给了 0.1 秒的宽容窗口——玩家离开平台后 0.1 秒内仍可起跳，这是消除'我明明按了'挫败感的标准做法。" |
| 2 | 0:50–1:10 | **切编辑器**：选中 Player，展开 Inspector 的 `土狼时间参数` / `动态重力缩放参数` / `跳跃变形参数`；展示 Scene 视图地面/墙面检测 Gizmo 射线 | "所有手感参数都在 Inspector 上可调。上升重力 0.9 倍、下落 1.3 倍——下落更快、跳跃更飘，是马里奥式的经典手感配方。" |
| 3 | 1:10–1:35 | **动态重力对比**（可做 A/B：把 riseGravityScale 改成 1.0 再跳一次） | "改一个数，手感立刻变化。这就是我把参数序列化而不是硬编码的原因。" |
| 4 | 1:35–2:10 | **3 连击连段**：完整打出 1-2-3 段，第三段带位移和更强顿帧；镜头稍微停顿让观众看到受击闪烁和伤害飘字 | "3 段连击，超 1 秒重置。第三段有 1.2 倍伤害和额外 5% 暴击加成，并触发独立时长的顿帧——让收招有'分量'。" |
| 5 | 2:10–2:50 | **反击 → 追击 完整循环**：等敌人出招（有反击窗口提示）→ 按反击 → 屏幕震动 + 冲击波 + 敌人被眩晕 → 立刻按攻击 → 自动冲刺到敌人并打出追击伤害 | "这是我最花心思的一段。反击成功后敌人进入眩晕并开启 0.5 秒追击窗口，窗口内攻击会自动追踪到目标。**这中间有三个状态机在交接**：反击状态关闭敌人窗口、开启无敌帧，追击状态接管无敌并临时禁用顿帧，否则顿帧的 FreezeAll 会把突进卡住。" |
| 6 | 2:50–3:05 | **切编辑器**：`Player_Combat` 的 Inspector，展示"顿帧设置"分组 6 个开关 | "顿帧我做了逐项开关：暴击、反击、追击、连击收招、跳劈各有独立时长。因为不是每种命中都该有顿帧——追击加顿帧会破坏流畅性，所以我默认关掉它，并且代码里显式禁用。" |
| 7 | 3:05–3:35 | **顿帧慢动作特写**（关键：一段 0.08 秒的顿帧用慢放呈现） | "顿帧不是简单冻结。它是三段式：前 50% 完全静止，后 50% 用 AnimationCurve 从 0 抬升到 1，而且**只冻结攻击者和受击者**，其他敌人照常行动。全局顿帧才动 Time.timeScale。" |
| 8 | 3:35–4:05 | **元素战斗**：火（敌人身上持续灼烧+跳字）→ 冰（敌人变蓝+动作明显变慢）→ 雷（连续攻击累计电荷，达到阈值落雷） | "三种元素是三套完全不同的机制：火是固定总伤的 DoT，冰是受抗性影响的减速，雷是累计电荷触发的爆发。火伤用 `ignoreInvincibility` 绕开受击无敌帧，否则自己就把自己的 DoT 顶掉了。" |
| 9 | 4:05–4:40 | **技能树 UI 操作**：打开面板 → 展示节点/连线/前置关系 → 花技能点解锁一个分支 → 关闭面板 → 立刻实战释放 | "技能树是节点+连线+前置/冲突校验。技能数据全部是 ScriptableObject，35 个技能数据资产。" |
| 10 | 4:40–5:20 | **领域展开大招**（最炫的一段，可反复播 2–3 次不同分支） | "领域展开有三种分支：减速领域、回响领域、碎片领域。选回响领域时，领域每 0.5 秒对随机目标生成一个时间回响残影；选碎片领域就改成生成追踪碎片。**这是技能系统之间的互相调用**——领域技能持有的是时间回响和碎片的引用。" |
| 11 | 5:20–5:50 | **切编辑器 + 代码**：打开 `Skill_DomainExpansion.cs`，高亮 `CastSpell()` 和 `upgradeType` 分支判断 | 讲升级分支的实现方式（见第 3 部分 A-4） |
| 12 | 5:50–6:30 | **Boss 战 完整一轮**：进场 → 大跳前摇 + 落点预警 → 召唤小史莱姆 → 打到 30% 血触发分裂 → 两只史莱姆 → 打掉子体，本体狂暴（冲刺更快）→ 打到 1% 触发濒死隐身回血+召唤 | "Boss 是动作池调度器，不是状态机驱动动作，而是按冷却+距离条件筛选可用动作。四个阶段：常态、分裂、僵直、隐身。**最难的是边界情况**：如果玩家伤害太高，一击同时跨过 30% 分裂线和致死线，我这里会拦下来先走分裂流程，否则玩家会跳过整个第二阶段。濒死保命是一次性的，且有召唤次数上限，防止无限刷怪。" |
| 13 | 6:30–7:10 | **装备/背包**：打开背包 → 拖拽装备到槽位 → 属性面板实时变化 → 展示一件带 4 词缀的传说装备（前缀+后缀、含双刃负面） | "装备词缀是程序化生成的。稀有度决定词缀数量和可选池深度，还有'稀有度调性表'——史诗装只出正面词缀，稀有装强制全双刃，用代价换强度。" |
| 14 | 7:10–7:35 | **掉落演示**：连续击杀精英，地面抛物线散落物品 → 落地停住 → 走过去自动拾取 → DOTween 拖尾飞向 UI → 背包格出现（稀有度底色 + 数量角标） | "掉落走 `ILootable` 接口，把'谁掉'和'怎么掉'解耦。物品从敌人头顶 2 米生成，左右交替给水平初速，弧线交给 Rigidbody2D 重力。**同一批掉落之间用 `Physics2D.IgnoreCollision` 互不挤飞**；`hasLanded` 判定防空中拾取；背包满时物品留在地上不销毁，不会丢东西。" |
| 15 | 7:35–8:10 | **切到 Unity 编辑器 → Tools → 任务树编辑器**：GraphView 画布上拖拽任务节点、连线表示前置、点节点右侧 Inspector 编辑、点"排列"自动布局、点"保存全部"→ **打开 SO 资产展示 `prerequisiteQuestIds`/`followUpQuestIds` 被写入** | "这是我自研的任务编辑器，基于 Unity 的 GraphView。策划不用碰代码，在画布上连任务链就行。**关键设计是图不落盘**——编辑器把图投影到每个 SO 的两个字符串列表上（入边 → 前置列表，出边 → 后续列表），资产是唯一事实源，图只是视图。所以不存在'图和数据不同步'的问题。" |
| 16 | 8:10–8:35 | **任务全流程**：找村长按 F 接取 → 杀骷髅 → HUD 追踪条实时 +1 → 回村长一次点击"提交并领取奖励"自动过渡到阶段二 → 收集血瓶提交（展示物品被扣除）→ 领最终奖励 | "任务进度由事件驱动推送（`QuestEvents` 总线），但**阶段推进是拉取式的**——只有找 NPC 对话才能进下一阶段。这是刻意的：叙事节奏该由玩家和 NPC 的交互控制，不该自动跳。另外阶段奖励用 `claimedStageRewards` 防重复领取。" |
| 17 | 8:35–9:05 | **存档往返（限定版）**：存档点按 F（展示提示淡入+粒子+音效）→ 装备一件带 4 词缀的传说装备（记住词缀名与数值）→ 学一个技能并绑定槽位 → 任务推进 1 步 → 切场景 → 回主菜单读档 → **展示装备词缀/稀有度/槽位、技能等级与绑定、任务阶段全部还原** → 用资源管理器打开 `slot_0.json` 展示 pretty-print JSON | "Unity 的 `JsonUtility` 不能序列化 ScriptableObject 引用——存对象引用重新编译就断，存 instanceID 每次运行都变。我的方案是**只存稳定的业务 ID 字符串**，读档时从 Resources 反查重建，所以存档和资产解耦。恢复顺序也有依赖：技能必须先于背包，因为背包容量由被动技能决定。" |
| 18 | 9:05–9:25 | **技能树门控对比**：同一段地形，点亮二段跳前跳不上去 → 打开技能树点亮 → 回去跳上去。顺带展示前置链校验和冲突分支递归锁定 | "银河城的核心是'获得能力→打开新区域'。我没单独写 `AbilityGate` 组件，而是把门控收敛到技能树的前置依赖上——`preSkillRequirements` 支持跨技能树指定前置技能和最低等级，冲突节点会递归锁定整棵子树。**门控只有一份真源**，不会出现'技能树点了但场景门没开'的不一致。" |
| 19 | 9:25–9:45 | **切到 CSV 导入窗口**：展示 xlsx→csv→SO 流程，一键导入（展示界面即可，不必真跑） | "数据管线是 xlsx → CSV → ScriptableObject。物品、掉落表、任务、属性、词缀、配方全走这条管线，目前 114 个数据资产，带中文表头映射层。" |
| 20 | 9:45–10:00 | **收尾**：架构分层图 + 代码量统计 + 后续规划 | 总结技术关键词，提一句后续规划（传送门网络、能力门组件化），然后收 |

### 录制实操建议

1. **先录玩法，再补编辑器镜头**。玩法镜头容易被意外打断（死亡、卡位），编辑器镜头一次就能过。
2. **慢放只用在顿帧和反击**。其他都原速。顿帧 0.08 秒在 60fps 下只有 5 帧，不放慢观众根本看不到。
3. **伤害飘字和受击闪烁要给特写**。这些是"我能感知到打击感"的视觉证据。
4. **Boss 战必须一次打完一个完整阶段转换**。剪辑碎的 Boss 战没有说服力。
5. **不要展示未实现的系统**。宁可视频短 1 分钟。

---

## 第 3 部分：技术力深度分析（面试讲解稿）

这一部分是核心。每个系统我都按 **「做了什么 → 怎么实现的（可指认的类/方法）→ 技术难点在哪 → 面试官可能怎么问」** 组织。

---

### A-1 核心架构：组件式有限状态机（你最该讲透的一项）

你记得"有一个有限状态机"——实际上**项目里有三套独立的 FSM**，这是比你记忆更强的点：

| FSM | 宿主 | 状态数 | 用途 |
|-----|------|--------|------|
| 玩家 FSM | `Player.cs` | **14 个具体状态** | 移动/攻击/技能/反击/死亡 |
| 敌人 FSM | `Enemy.cs` | **6 个状态** | 巡逻/追击/攻击/眩晕/死亡 |
| Boss 阶段机 | `Boss_SlimeKing.cs` | **4 个阶段** | 常态/分裂/僵直/隐身 |

#### 实现方式（可直接指认）

`StateMachine.cs` 只有 32 行，是一个**纯 C# 类，不是 MonoBehaviour**：

```csharp
public class StateMachine
{
    public EntityState currentState { get; private set; }
    public bool canChangeSate;               // ← 状态机总闸，Boss 登场前用它冻结 AI

    public void Initialize(EntityState startState) { canChangeSate = true; currentState = startState; currentState.Enter(); }
    public void ChangeState(EntityState newState) { if (canChangeSate == false) return; currentState.Exit(); currentState = newState; currentState.Enter(); }
    public void UpdateActiveState() => currentState.Update();
    public void SwitchOffStateMachine() => canChangeSate = false;
}
```

状态基类是 `EntityState`（56 行纯 C#），扮演**模板方法模式**的角色：

```csharp
public abstract class EntityState
{
    protected float stateTimer;     // 状态计时器，Enter 后可用于"持续 N 秒"
    protected bool triggerCalled;   // 动画事件回调标志位

    public virtual void Enter() { anim.SetBool(animBoolName, true); triggerCalled = false; }
    public virtual void Update() { stateTimer -= Time.deltaTime; UpdateAnimationParaeters(); }
    public virtual void Exit() { anim.SetBool(animBoolName, false); }

    // 动画事件回调入口：Animator 在关键帧回调 Player.CurrentState_AnimationTriggers()
    public void AnimationTriggers() => triggerCalled = true;
}
```

继承链（`production/review/player.md` 已核实）：

```
EntityState (abstract)
 └─ PlayerState (abstract)              ← 承载全局打断输入（冲刺/领域展开/反击）
     ├─ Player_GroundedState (abstract) ← 地面态
     │   ├─ Player_IdleState
     │   └─ Player_MoveState
     ├─ Player_AiredState (abstract)    ← 空中态
     │   ├─ Player_JumpState
     │   ├─ Player_DoubleJumpState
     │   ├─ Player_FallState
     │   └─ Player_JumpAttackState
     ├─ Player_WallSlideState / Player_WallJumpState
     ├─ Player_DashState
     ├─ Player_BasicAttackState
     ├─ Player_CounterAttackState / Player_CounterChaseState
     ├─ Player_DomainExpansionState
     └─ Player_DeadState
```

`EnemyState` 为平行分支，承载敌人专用的 `DirectionToPlayer()`、动画触发、朝向处理。

#### 技术难点与设计亮点

**难点 1：状态类需要访问宿主，但不能是 MonoBehaviour。**
状态是 `new` 出来的纯 C# 对象（`Player.Awake()` 里一次性构造 14 个），通过构造函数注入宿主引用：

```csharp
// PlayerState 构造函数：把宿主的公共依赖一次性抓下来，避免状态内 GetComponent
public PlayerState(Player player, StateMachine stateMachine, string animBoolName) : base(stateMachine, animBoolName)
{
    this.player = player;
    anim = player.anim;      // 缓存，状态内不再 GetComponent
    rb = player.rb;
    stats = player.stats;
    skillManager = player.skillManager;
}
```

> **面试讲解点**：为什么不用 MonoBehaviour 做状态？因为 MonoBehaviour 意味着每个状态都是组件、都要挂在 GameObject 上、都要走 Unity 的生命周期和序列化。纯 C# 对象轻量、可单元测试、构造即完成依赖注入，没有 `GetComponent` 开销。

**亮点 2：全局打断输入放在基类 `PlayerState.Update()`。**
这是很聪明的设计——冲刺、领域展开、反击这三种"任何状态都能切进去"的操作只写一次：

```csharp
public override void Update()
{
    base.Update();

    // 冲刺：多数状态可强行切入
    if ((GameInput.GetKeyDown(GameInput.Action.Dash) || (player.inputBuffer != null && player.inputBuffer.HasDashBuffer())) && CanDash())
    {
        if (player.inputBuffer != null) player.inputBuffer.ClearDashBuffer();
        skillManager.dash.StartSkillCooldown();
        stateMachine.ChangeState(player.dashState);
    }

    // 领域展开、反击同理
}
```

而"不能反击的状态"用**黑名单**显式排除：

```csharp
public bool CanUseCounter()
{
    if (player.IsDead) return false;
    var cur = stateMachine.currentState;
    // 不能反击的状态：死亡、领域展开（无法行动）、反击/追击中（防止重入）
    if (cur == player.deadState || cur == player.domainExpansionState ||
        cur == player.counterAttackState || cur == player.counterChaseState)
        return false;
    return true;
}
```

> **面试讲解点**：为什么用黑名单而不是白名单？因为"反击"应该是一种**通用能力**——新增状态时默认可用，只有明确不该用的才排除。白名单会导致每加一个状态就要记得去加一行，容易漏。这是"默认开放、显式关闭"的设计取向。

**亮点 3：动画事件驱动攻击判定，而不是计时器。**
攻击的"命中帧"由 Animator 的事件回调触发，而不是猜测时间：

```
Animator 关键帧事件 → Player.CurrentState_AnimationTriggers()
  → Entity.CurrentState_AnimationTriggers()
    → stateMachine.currentState.AnimationTriggers()
      → EntityState.AnimationTriggers() → triggerCalled = true
```

状态在自己的 `Update()` 里检查 `triggerCalled` 决定何时收招/切换。

> **面试讲解点**：这样动画师调整帧率/加帧，判定点自动跟着走，不需要改代码。这是把"表现"和"逻辑"解耦的正确方式。

**亮点 4：`canChangeSate` 总闸用于 Boss 登场冻结。**
`Boss_SlimeKing.Start()` 里：

```csharp
stateMachine.Initialize(idleState);
stateMachine.SwitchOffStateMachine();   // 冻结 FSM，Boss 站在场上不动、不进战斗
// ... BossEncounter 在 Intro→Fighting 时调用 BeginFight() 才解冻
```

> **面试讲解点**：Boss 需要在玩家进入场地、播完入场演出后才开始行动。用状态机总闸而不是 `enabled = false`（那会连 Update 一起停掉，血条、特效都不动了）。

#### 面试官可能怎么问

| 问题 | 回答要点 |
|------|---------|
| 为什么用状态机而不是行为树？ | 玩家控制是**强状态**的：你不能在冲刺时同时走路。状态机的"互斥"语义天然贴合。行为树更适合"多个目标并行权衡"的 AI。Boss 我实际用的是**动作池调度器**（见 A-7），因为它需要的是"从可用动作里挑一个"，而非状态互斥 |
| 状态之间怎么通信？ | 事件为主。`Entity` 暴露 `OnFlipped`/`OnEntityDead`，`Entity_Combat` 暴露 `OnAttackHitResult`/`OnAttackHitWithIndex`，`Entity_Health` 暴露 `OnHealthUpdate`。状态在 `Enter` 订阅、`Exit` 退订，配对严格 |
| 状态机的缺点是什么？ | 状态数增长时转换会爆炸。我这个项目已经 14 个状态，`Player.cs` 里集中维护状态表 + 各状态的转换点。如果继续长，我会把转换抽成配置表，或者引入分层状态机（HFSM） |
| 你怎么保证不会漏退订？ | 每个状态的 `Enter`/`Exit` 严格配对。这是我在代码审查里专门验证过的一项——`production/review/player.md` 记录了"Enter/Exit 的事件订阅-退订配对严格（无泄漏）" |

---

### A-2 手感工程：土狼时间 + 输入缓冲 + 动态重力

这部分是"证明你真的玩过游戏、知道手感从哪来"的地方。

#### 土狼时间（Coyote Time）

```csharp
[Header("土狼时间参数")]
public float coyoteTime = 0.1f;         // 离开平台后仍可起跳的时间
private float lastGroundedTime;         // 最后一次在地面的时间

public void UpdateLastGroundedTime() => lastGroundedTime = Time.time;
public bool CanUseCoyoteTime() => Time.time - lastGroundedTime <= coyoteTime;
```

**原理**：玩家离开平台边缘后，仍保留 0.1 秒的"还能跳"窗口。

> **面试讲解点**：玩家按下跳跃到角色实际离开地面，中间有输入延迟和物理帧误差。不给宽容窗口，玩家会觉得"我明明按了却没跳"。这是 2D 平台游戏的标准手感补偿，空洞骑士、蔚蓝都有。

#### 输入缓冲（5 路并行）

`Player_InputBuffer.cs` 是独立的 MonoBehaviour，维护 5 条独立缓冲通道：

```csharp
[Header("缓冲时间参数")]
public float jumpBufferTime = 0.1f;
public float doubleJumpBufferTime = 0.1f;
public float dashBufferTime = 0.1f;
public float attackBufferTime = 0.1f;
public float counterAttackBufferTime = 0.1f;

private float jumpBufferTimer, attackBufferTimer, dashBufferTimer,
              counterAttackBufferTimer, doubleJumpBufferTimer;

// 按下时打时间戳
public void AddJumpBuffer() => jumpBufferTimer = jumpBufferTime;
// 状态查询
public bool HasJumpBuffer() => jumpBufferTimer > 0;
// 消费后清除（防止一次输入触发两次）
public void ClearJumpBuffer() => jumpBufferTimer = 0;
```

`Player.HandleInputBuffer()` 每帧把输入写进缓冲：

```csharp
if (GameInput.GetKeyDown(GameInput.Action.Jump))
{
    if (isOnGround) inputBuffer.AddJumpBuffer();
    else            inputBuffer.AddDoubleJumpBuffer();   // 空中的跳跃进另一条通道
}
if (GameInput.GetKeyDown(GameInput.Action.Attack))       inputBuffer.AddAttackBuffer();
if (GameInput.GetKeyDown(GameInput.Action.Dash))         inputBuffer.AddDashBuffer();
if (GameInput.GetKeyDown(GameInput.Action.CounterAttack)) inputBuffer.AddCounterAttackBuffer();
```

> **面试讲解点**：为什么跳跃要分两条通道？因为`跳`和`二段跳`是不同的合法动作。如果共用一条，玩家在落地前 0.1 秒按跳，落地后会被当成"地面跳"立刻消耗掉，二段跳就丢了。分开之后，地面跳缓冲落地生效，空中跳缓冲由空中状态消费。
>
> **"消费后清除"很关键**：缓冲只应该生效一次。不 `Clear` 的话，0.1 秒内跨多个状态会重复触发。

#### 动态重力

```csharp
[Header("动态重力缩放参数")]
public float riseGravityScale = 0.9f;                  // 上升时重力 ×0.9（更飘）
public float fallGravityScale = 1.3f;                  // 下落时重力 ×1.3（更快落地）
public float doubleJumpRiseGravityScale = 1f;
public float doubleJumpFallGravityScale = 1.6f;        // 二段跳后下落更快

private void UpdateDynamicGravity()
{
    if (disableDynamicGravity) return;

    if (isOnGround) { rb.gravityScale = originalGravityScale; return; }

    if (rb.linearVelocity.y > 0)   // 上升
        rb.gravityScale = originalGravityScale * (isDoubleJumping ? doubleJumpRiseGravityScale : riseGravityScale);
    else                            // 下落
        rb.gravityScale = originalGravityScale * (isDoubleJumping ? doubleJumpFallGravityScale : fallGravityScale);
}
```

> **面试讲解点**：恒定重力的问题是跳跃弧线对称，玩家会觉得"飘"且"落地慢"。上升减速、下落加速，跳跃顶点更快、下落更干脆，落地反馈更明确。二段跳用更极端的 1.6 倍，是因为二段跳通常用来救场，需要快速回到可控状态。
>
> `disableDynamicGravity` 这个开关是给状态用的——领域展开上升、冲刺期间需要自定义速度，不能让重力干扰。

#### 跳跃压扁拉伸（Squash & Stretch）

```csharp
public void JumpSquashAndStretch()
{
    squashStretchTween?.Kill();      // 先杀掉上一次，防止叠加
    var seq = DOTween.Sequence();
    seq.Append(sr.transform.DOScaleY(jumpSquashScale, jumpSquashDuration).SetEase(Ease.OutQuad));   // 压扁
    seq.Join(sr.transform.DOScaleX(1f / jumpSquashScale, jumpSquashDuration).SetEase(Ease.OutQuad)); // X 反向补偿
    seq.Append(sr.transform.DOScaleY(jumpStretchScale, jumpStretchDuration).SetEase(Ease.OutQuad)); // 拉伸
    seq.Join(sr.transform.DOScaleX(1f / jumpStretchScale, jumpStretchDuration).SetEase(Ease.OutQuad));
    seq.Append(sr.transform.DOScale(Vector3.one, 0.05f));
    squashStretchTween = seq;
}
```

> **面试讲解点**：`DOScaleX(1f / scale)` 是**体积守恒补偿**——Y 压扁到 0.9，X 就放大到 1/0.9。不这么做角色会看起来"变小"而不是"被压扁"。这是动画十二法则里的 squash & stretch，用 Tween 而不是动画帧实现，可以随时调参。

---

### A-3 战斗系统：7 步伤害管道

#### 管道全貌（可逐行指认）

`Entity_Combat.ProcessAttackOnTarget()` → `Entity_Health.TakeDamage()`：

```csharp
// ① 范围检测（Physics2D.OverlapCircleAll）
public Collider2D[] GetDectectedCollders() =>
    Physics2D.OverlapCircleAll(targetCheck.position, targetCheckRadius, whatIsTarget);

// ② 逐目标处理
protected virtual void ProcessAttackOnTarget(Collider2D target)
{
    IDamgable damgable = target.GetComponent<IDamgable>();     // 接口解耦：玩家/敌人/可破坏物都走同一管道
    if (damgable == null) return;

    AttackData attackData = CalculateAttackData();              // 伤害计算（虚方法，子类可覆写）
    bool targetGotHit = damgable.TakeDamage(
        attackData.phyiscalDamage, attackData.elementalDamage,
        attackData.element, transform, attackData.isCrit);

    if (targetGotHit)
    {
        OnTargetHit(target, attackData);         // 命中回调（音效/顿帧/屏幕震动）
        ApplyElementalEffect(target, attackData); // 元素状态施加
        CreateHitVFX(target, attackData.isCrit, attackData.element); // 特效

        // 通知自身 Enemy 的词缀（吸血、闪电导体等）
        var dealerEnemy = GetComponentInParent<Enemy>();
        if (dealerEnemy != null)
            dealerEnemy.ReportDealtDamage(attackData.phyiscalDamage + attackData.elementalDamage);
    }
}
```

`Entity_Health.TakeDamage()` 的完整判定顺序：

```csharp
public virtual bool TakeDamage(float physicalDamage, float elementalDamage, ElementType element,
                               Transform damageDealer, bool isCrit = false, bool ignoreInvincibility = false)
{
    if (isDead) return false;                                   // ③ 死亡检查

    if (AttackEvaded()) { /* ... */ return false; }              // ④ 闪避判定

    // ⑤ 受击无敌帧（DoT 用 ignoreInvincibility 豁免）
    if (ignoreInvincibility == false && invincibleDuration > 0f && Time.time < invincibleUntil)
        return false;

    Entity_Stats attackerStats = damageDealer.GetComponent<Entity_Stats>();
    float armorReduction = attackerStats != null ? attackerStats.GetArmorReduction() : 0;
    float mitigation  = entityStats != null ? entityStats.GetArmorMitigation(armorReduction) : 0;
    float resistance  = entityStats != null ? entityStats.GetElementalResistance(element) : 0;

    // ⑥ 减伤计算：物理走护甲，元素走抗性，互不干扰
    float physicalDamageTaken  = physicalDamage  * (1 - mitigation);
    float elementalDamageTaken = elementalDamage * (1 - resistance);

    TakeKnockBack(damageDealer, physicalDamageTaken);
    ReduceHP(physicalDamageTaken, elementalDamageTaken, element, isCrit);  // ⑦ 扣血 + 事件通知

    // 命中后才进无敌（DoT 不刷新无敌帧，避免被烧期间近乎无敌）
    if (canBeTakedDamage && ignoreInvincibility == false && invincibleDuration > 0f)
        invincibleUntil = Time.time + invincibleDuration;

    lastDamageTaken = physicalDamageTaken + elementalDamageTaken;
    // ...
}
```

#### 数值公式（真实代码，非文档）

```csharp
// Entity_Stats.cs —— 主属性 → 派生属性
GetBasePhyiscalDamage() = offense.phyiscalDamage + major.strength * 0.5f
GetBaseCritChance()     = offense.critChance + major.agility * 0.5f
GetBaseCritPower()      = offense.critPower + major.strength * 1f     // 后续 /100
GetBaseArmor()          = defense.armor + major.vitality * 0.5f
GetMaxHP()              = resources.maxHP + major.vitality * 5 + 元素之心 * 50

// 护甲减免（注意：是"护甲穿透"消减有效护甲）
GetArmorMitigation(armorReduction):
    reductionMultiplier = Clamp(1 - armorReduction, 0, 1)
    effectiveArmor      = GetBaseArmor() * reductionMultiplier
    mitigation          = effectiveArmor / (effectiveArmor + 200)
    return Clamp(mitigation, 0, 0.6)          // ← 上限 60%，源码注释说明"原 0.75 过高，下调"

// 元素抗性：智力每点 +0.25，上限 75%
GetElementalResistance(element) = Clamp(baseRes + major.intelligence * 0.25f, 0, 75) / 100

// 闪避：敏捷每点 +0.5，上限 75%
GetEvasion() = Clamp(defense.evasion + major.agility * 0.5f, 0, 75)
```

> **面试讲解点**：护甲用 `armor / (armor + K)` 这种**递减公式**而不是线性减伤。常数 K=200 是关键——它决定了"多少护甲能减多少伤"的曲线形状。线性减伤的问题是后期护甲堆到 100% 就无敌了；递减公式永远逼近但达不到上限，我另外加了 60% 硬上限兜底。
>
> 为什么用"护甲穿透"而不是"减护甲值"？因为穿透整段公式是**乘法**的（`effectiveArmor = armor * (1 - reduction)`），不会被递减曲线吃掉收益。破甲 50% 就恰好让有效护甲减半，直观且好平衡。

#### 暴击与特殊攻击加成

`Player_Combat.CalculateAttackData()` 覆写基类：

```csharp
protected override AttackData CalculateAttackData()
{
    float damageMultiplier = GetDamageMultiplier();   // 1.0 / 1.2
    float critChanceBonus  = GetCritChanceBonus();    // 0 / +5

    float physDamage = stats.GetPhyiscalDamage(basicAttacksData.phyiscal);
    float elemDamage = stats.GetElementalDamage(stats.InputElement, out ElementType element, basicAttacksData.elemental);

    bool isCrit = stats.CalculateCritStatus(critChanceBonus);
    if (isCrit)
    {
        float critPower = stats.GetCritPower();
        physDamage *= critPower;
        elemDamage *= critPower;
    }

    physDamage *= damageMultiplier;
    elemDamage *= damageMultiplier;
    // ...
}

// 特殊攻击类型用 switch 表达式而非 if 链
private float GetDamageMultiplier() => currentSpecialAttack switch
{
    SpecialAttackType.JumpAttack       => jumpAttackDamageMultiplier,        // 1.2
    SpecialAttackType.ChaseAttack      => chaseAttackDamageMultiplier,       // 1.2
    SpecialAttackType.ThirdComboAttack => thirdComboAttackDamageMultiplier,  // 1.2
    _ => 1f
};
```

> **面试讲解点**：暴击加成我放在 `CalculateCritStatus(critChanceBonus)` 的参数里，而不是改玩家的属性值。因为"这次攻击 +5% 暴击"是**单次修正**，不是永久增益。如果去改 `Stat`，就要记得改回来，很容易漏。

#### 反击 → 追击循环（最复杂的连段逻辑）

`Player_Combat.CounterAttackPerformed()`：

```csharp
public bool CounterAttackPerformed()
{
    bool hasPerformedCounter = false;
    chaseTarget = null;

    foreach (var target in GetDectectedCollders())
    {
        ICounterable counterable = target.GetComponent<ICounterable>();
        if (counterable == null) continue;
        if (!counterable.IsInCounterTime) continue;          // 敌人是否处于可反击窗口

        hasPerformedCounter = true;

        // ① 立即关闭敌人窗口 —— 防止 HitStop 延迟期间被二次反击
        target.GetComponent<Enemy>()?.EnableCounterTime(false);

        bool targetCanBeChased = counterable.CanBeChased;

        // ② 音效 + 视觉（屏幕震动 + 冲击波）
        AudioManager.Instance?.PlayCounterSuccessSfx();
        AudioManager.Instance?.PlayCounterHitSfx();
        player.VFX?.ShakeScreenForCounter();
        player.VFX?.DoCounterVisuals(target.transform.position);

        // ③ 立即眩晕敌人 —— 中断攻击动画，防止 HitStop 延迟期间被攻击
        counterable.HandleCounter(counterKnockbackMultiplier);

        if (targetCanBeChased) chaseTarget = target.transform;

        // ④ 延迟 1 帧再顿帧（等粒子生成）
        if (enableCounterHitStop) StartCoroutine(DelayedHitStopEffect(target.gameObject));
    }

    // 反击失败进冷却；成功则取消冷却，可以立刻再反击
    if (!hasPerformedCounter) ActivateCounterCooldown();
    else                      counterCooldownActive = false;

    return hasPerformedCounter;
}
```

无敌帧**独立于状态机生命周期**（`Player_Combat` 自己计时）：

```csharp
private float counterInvincibleRemaining;
public void StartCounterInvincibility() { counterInvincibleRemaining = counterInvincibleDuration; player.health.canBeTakedDamage = false; }
public void CancelCounterInvincibility() { counterInvincibleRemaining = 0f; }   // 追击接管时清零，避免双重复位冲突
public void UpdateCounterInvincibility()
{
    if (counterInvincibleRemaining <= 0f) return;
    counterInvincibleRemaining -= Time.deltaTime;
    if (counterInvincibleRemaining > 0f) return;
    counterInvincibleRemaining = 0f;
    player.health.canBeTakedDamage = true;
}
```

追击状态**临时禁用顿帧**（这是最容易忽略的坑）：

```csharp
// Player_CounterChaseState：顿帧的 FreezeAll 会把突进卡住，所以追击期间显式关掉
// 并在 Update 里每帧强制 EndHitStop() 清残留
```

> **面试讲解点（这段最出彩）**：反击看起来是个简单动作，实际有 **4 个子系统在交接**：反击窗口、眩晕、无敌帧、追击窗口。
>
> 我踩过三个坑：
> 1. **顿帧延迟期间的二次反击**——顿帧是延迟 1 帧生效的，如果不立刻关闭敌人的可反击窗口，这一帧里敌人还能被"再反击一次"。
> 2. **顿帧的 FreezeAll 卡住追击突进**——追击时我把刚体约束改成 FreezeAll 冻结，结果速度也归零了，追击冲不出去。解决方案是追击期间禁用顿帧，并每帧强制清理残留。
> 3. **无敌帧的双重管理冲突**——反击给 0.35 秒无敌，追击状态自己也要管无敌，两边都去改 `canBeTakedDamage` 会互相打架。我用 `CancelCounterInvincibility()` 让追击接管时先清零反击的计时器，保证只有一个 owner。

#### 面试官可能怎么问

| 问题 | 回答要点 |
|------|---------|
| 为什么用 `Physics2D.OverlapCircleAll` 而不是 Collider 触发器？ | 攻击判定的时机由动画事件控制，是**离散的瞬时判定**，不是持续性接触。Overlap 可以在动画命中帧那一瞬间采样一次，语义更清晰。触发器的持续检测会导致"在攻击盒里多站几帧就被打多次" |
| 这个方案有什么代价？ | `OverlapCircleAll` 每次调用分配新数组，有 GC。我的审查报告里记录了这个问题——每次攻击最多调用 3 次。改进方向是用 `OverlapCircleNonAlloc` 复用缓冲区 |
| 伤害管道怎么扩展到新类型？ | 走 `IDamgable` 接口。玩家、敌人、可破坏物都实现它。新增可受伤对象只需实现接口，不用改攻击方代码 |
| 闪避和无敌帧的优先级？ | 闪避先判定（`AttackEvaded` 在最前），闪避成功直接返回，连无敌帧都不消耗。这个顺序是有意的——闪避成功不该浪费无敌帧 |

---

### A-4 顿帧系统（HitStop）—— 双轨三段式

`HitStopManager.cs`（247 行）是整个项目里我认为**最能体现"手感工程经验"**的一个文件。

#### 双轨设计

| 类型 | 机制 | 影响范围 | 用途 |
|------|------|---------|------|
| **全局顿帧** | 直接改 `Time.timeScale` | 全场（含 BGM、UI 动画） | 大招、Boss 击杀等重大时刻 |
| **局部顿帧** | 逐目标设置 `anim.speed` + `Rigidbody2D.constraints` | 只有攻击者 + 受击者 | 常规命中、暴击、反击 |

```csharp
public void TriggerGlobalHitStop(float duration = -1)
{
    if (isGlobalHitStopActive) return;                    // 防重入
    float actualDuration = duration > 0 ? duration : globalHitStopDuration;
    originalTimeScale = Time.timeScale;                   // 保存原值
    Time.timeScale = globalHitStopTimeScale;              // 通常是 0.01
    isGlobalHitStopActive = true;
    globalHitStopTimer = actualDuration;
    StartCoroutine(ResumeAfterHitStop(actualDuration));
}
```

局部顿帧用**字典逐目标管理**，因为可能同时有多个目标被冻结：

```csharp
private List<IHitStopable> hitStopTargets = new List<IHitStopable>();
private Dictionary<IHitStopable, HitStopData> hitStopDataMap = new Dictionary<IHitStopable, HitStopData>();

private class HitStopData { public float totalDuration, elapsedTime, originalSpeed; public HitStopPhase currentPhase; }
private enum HitStopPhase { Freeze, SlowRecovery, FastRecovery }
```

#### 三段式恢复曲线（核心）

```csharp
[Header("三段式顿帧配置")]
[SerializeField] private float freezePhaseRatio     = 0.5f;   // 冻结占 50%
[SerializeField] private float slowRecoveryPhaseRatio = 0.3f; // 慢恢复占 30%
[SerializeField] private float fastRecoveryPhaseRatio = 0.2f; // 快恢复占 20%
[SerializeField] private AnimationCurve recoveryCurve = AnimationCurve.EaseInOut(0, 0, 1, 1);

// Update 里逐目标推进
data.elapsedTime += Time.unscaledDeltaTime;      // ← 用 unscaledDeltaTime！timeScale=0 时 deltaTime 也是 0
float freezeDuration = data.totalDuration * freezePhaseRatio;
float slowRecoveryDuration = data.totalDuration * slowRecoveryPhaseRatio;
float fastRecoveryDuration = data.totalDuration * fastRecoveryPhaseRatio;

if (data.elapsedTime >= data.totalDuration) { target.EndHitStop(); toRemove.Add(target); }
else
{
    float currentSpeed = 0f;
    if (data.elapsedTime <= freezeDuration)
    {
        data.currentPhase = HitStopPhase.Freeze;
        currentSpeed = localHitStopTimeScale;                             // 0 → 完全静止
    }
    else if (data.elapsedTime <= freezeDuration + slowRecoveryDuration)
    {
        data.currentPhase = HitStopPhase.SlowRecovery;
        float phaseProgress = (data.elapsedTime - freezeDuration) / slowRecoveryDuration;
        currentSpeed = recoveryCurve.Evaluate(phaseProgress) * 0.3f;      // 曲线 0 → 0.3
    }
    else
    {
        data.currentPhase = HitStopPhase.FastRecovery;
        float phaseProgress = (data.elapsedTime - freezeDuration - slowRecoveryDuration) / fastRecoveryDuration;
        currentSpeed = recoveryCurve.Evaluate(phaseProgress) * 0.7f + 0.3f; // 曲线 0.3 → 1.0
    }
    target.SetAnimationSpeed(currentSpeed);
}
```

受击侧的 `Entity.SetAnimationSpeed()` 同步冻结物理：

```csharp
public void SetAnimationSpeed(float speed)
{
    if (anim != null) anim.speed = speed;

    if (isHitStopActive && rb != null)
    {
        if (speed <= 0)
        {
            rb.linearVelocity = Vector2.zero;
            rb.constraints = RigidbodyConstraints2D.FreezeAll;    // 完全冻结
        }
        else
        {
            if (rb.constraints == RigidbodyConstraints2D.FreezeAll)
                rb.constraints = savedConstraints;                // 解冻
            rb.linearVelocity = savedVelocity * Mathf.Clamp01(speed); // 速度按恢复比例还原
        }
    }
}
```

`Entity.StartHitStop()` 保存/恢复状态：

```csharp
private IEnumerator HitStopCo(float duration)
{
    isHitStopActive = true;
    originalAnimSpeed = anim.speed;
    anim.speed = hitStopTimeScale;

    savedVelocity = rb.linearVelocity;          // 保存速度
    savedConstraints = rb.constraints;          // 保存约束
    rb.linearVelocity = Vector2.zero;
    rb.constraints = RigidbodyConstraints2D.FreezeAll;

    yield return new WaitForSecondsRealtime(duration);   // ← Realtime，不受 timeScale 影响

    rb.constraints = savedConstraints;          // 精确还原
    rb.linearVelocity = savedVelocity;
    anim.speed = originalAnimSpeed;
    isHitStopActive = false;
}
```

#### 逐项开关（配置化）

`Player_Combat` 的 Inspector 上是一整组顿帧开关：

```csharp
[Header("顿帧设置")]
[SerializeField] private bool  enableHitStop = true;
[SerializeField] private bool  enableHitStopOnCrit = true;        // 暴击顿帧
[SerializeField] private float critHitStopDuration = 0.15f;
[SerializeField] private bool  enableCounterHitStop = true;       // 反击顿帧
[SerializeField] private float counterHitStopDuration = 0.12f;
[SerializeField] private bool  enableChaseHitStop = false;        // 追击：默认关闭（会卡住突进）
[SerializeField] private float chaseHitStopDuration = 0.1f;
[SerializeField] private bool  enableLastComboHitStop = true;     // 连击收招
[SerializeField] private float lastComboHitStopDuration = 0.06f;
[SerializeField] private bool  enableJumpAttackHitStop = true;    // 跳劈
[SerializeField] private float jumpAttackHitStopDuration = 0.15f;
```

> **面试讲解点（这段是加分核心）**：
>
> 1. **为什么三段式而不是直接冻结→恢复？** 直接恢复会有一个"突兀地弹回原速"的顿挫感。三段式是：先完全静止（打击感来源）→ 用曲线从 0 缓慢抬到 0.3 倍（这个"慢速爬升"让玩家有时间看清命中反馈）→ 再快速回到 1.0（不留尾巴）。快恢复段占 20%，用曲线保证没有速度突变。
> 2. **为什么局部顿帧要用字典逐目标管理？** 因为一次攻击可能同时打中多个敌人，每个敌人的受击时刻可能不同，需要各自独立的恢复进度。用单个字段就只能有一个目标被冻结。
> 3. **`Time.unscaledDeltaTime` 是必须的**——如果全局顿帧把 `timeScale` 设成了 0，用 `Time.deltaTime` 计时器永远不会减少。这是很多人第一次做顿帧会踩的坑。
> 4. **为什么要保存/还原 `constraints` 和 `linearVelocity`？** 冻结用 `FreezeAll` 是最省事的，但解冻时必须把原来的约束和速度还回去，否则角色会变成"能穿墙"或者"突然静止"。我这里显式保存三个状态（`originalAnimSpeed`/`savedVelocity`/`savedConstraints`）并在两个出口（协程结束、`EndHitStop()`）都还原。
> 5. **为什么追击默认关顿帧？** 追击是"我主动冲过去"，中间插入冻结会打断连续感。而且 `FreezeAll` 会把刚体速度清零，追击直接冲不出去。这是个**负向设计决策**——不是每个命中都该有顿帧，克制比堆砌更重要。

#### 面试官可能怎么问

| 问题 | 回答要点 |
|------|---------|
| 全局顿帧会不会影响 UI 动画？ | 会，因为 `Time.timeScale` 影响所有用 `deltaTime` 的东西。我的 UI 里用 DOTween 的部分加了 `.SetUpdate(true)` 让它们走 unscaled 时间。这是一个取舍——大招时刻我希望 UI 也一起"顿"一下 |
| 多目标同时顿帧怎么办？ | 字典逐目标管理，各目标有独立的 `elapsedTime` 和恢复进度。同一目标被重复触发时会覆盖旧的 `HitStopData`（`hitStopDataMap[target] = data`） |
| 帧率低的时候顿帧准确吗？ | 不精确。计时是逐帧累加的，30fps 下 0.08 秒只有 2~3 帧，量化和抖动都明显。要高精度需要用时间戳而不是累加 |

---

### A-5 技能系统（技术含量最高的模块）

这是我认为**最值得花时间讲**的系统，因为它涉及继承、组合、数据驱动、跨技能调用四个层次。

#### 四路并行的架构

```
①  Skill_Base 组件（14 个技能实现）
        ↕ 被聚合
②  Player_SkillManager（11+ 技能组件的持有者，按 SkillType 索引）
        ↕ 被驱动
③  SkillDataManager（中心缓存：升级类型 → 等级 → LevelData）
        ↕ 被绑定
④  SkillSlotManager（5 个槽位，拖拽绑定/解绑/交换）
        +
    PassiveSkillManager（被动技能，解锁即生效，不占槽位）
        +
    SkillPointManager（技能点经济）
```

#### Skill_Base 的设计（115 行，模板方法 + 升级分支）

```csharp
public class Skill_Base : MonoBehaviour
{
    public Player_SkillManager skillManager { get; private set; }
    public DamageScaleData damageScaleData { get; private set; }
    public Player player { get; private set; }

    [Header("技能基础配置")]
    [SerializeField] public SkillType skillType;                    // 技能大类（Dash/Shard/TimeEcho...）
    [SerializeField] public SkillUpgradeType upgradeType;           // 当前激活的升级分支
    [SerializeField] public int currentLevel;
    [SerializeField] protected float cooldown;
    [SerializeField] protected int totalLevel = 0;
    private float lastTimeUsed;

    // 关键：记录"每个升级分支各自的等级"
    protected Dictionary<SkillUpgradeType, int> upgradeTypeLevels = new Dictionary<SkillUpgradeType, int>();

    protected virtual void Awake()
    {
        skillManager = GetComponentInParent<Player_SkillManager>();
        player       = GetComponentInParent<Player>();
        damageScaleData = new DamageScaleData();
        lastTimeUsed = lastTimeUsed - cooldown;   // 初始化到过去，保证开局即可用
    }

    // 升级数据注入（由 SkillSlotManager / SkillDataManager 调用）
    public virtual void SetSkillLevelData(SkillUpgradeType upgradeType, LevelData levelData, int level, bool updateUpgradeType = true)
    {
        totalLevel++;
        if (upgradeType != SkillUpgradeType.None)
        {
            if (updateUpgradeType) this.upgradeType = upgradeType;         // 切换分支
            else if (this.upgradeType == SkillUpgradeType.None) this.upgradeType = upgradeType; // 被动首次激活
        }
        upgradeTypeLevels[upgradeType] = level;
        this.currentLevel = level;
        this.cooldown = levelData.cooldown;
        this.damageScaleData = levelData.damageScaleData;
        ResetCooldown();
    }

    public virtual void TryUseSkill() { }                                  // 子类覆写

    public bool CanUseSkill()
    {
        if (upgradeType == SkillUpgradeType.None) return false;            // 未解锁
        if (IsOnCooldown()) return false;
        return true;
    }

    protected bool Unlocked(SkillUpgradeType upgradeToCheck) => upgradeType == upgradeToCheck;
    protected bool IsOnCooldown() => Time.time < lastTimeUsed + cooldown;
    public void StartSkillCooldown() => lastTimeUsed = Time.time;
    public void ReduceCooldown(float reductionAmount) => lastTimeUsed = Mathf.Max(0, lastTimeUsed - reductionAmount);
    public void ResetCooldown() => lastTimeUsed = Time.time - cooldown;
}
```

#### 升级分支的三种实现模式（这是设计亮点）

项目里 14 个技能不是用同一套模式写分支的，而是**按语义选了不同模式**。这个细节能直接体现设计判断力：

**模式一：类型即行为（子类覆写 + `upgradeType` 判断）**

`Skill_TimeEcho.cs` —— 一个技能承载两种完全不同的玩法（分身 / 治疗精灵）：

```csharp
public bool ShouldBeWisp()   // 允许变成治疗精灵
{
    return upgradeType == SkillUpgradeType.TimeEcho_HealWisp      // 恢复
        || upgradeType == SkillUpgradeType.TimeEcho_CleanseWisp   // 净化
        || upgradeType == SkillUpgradeType.TimeEcho_CooldownWisp; // 减少 CD
}

public int GetMaxAttacks()   // 攻击型分支决定能打几下
{
    if (upgradeType == SkillUpgradeType.TimeEcho_SingleAttack ||
        upgradeType == SkillUpgradeType.TimeEcho_ChanceToDuplicate) return 1;
    if (upgradeType == SkillUpgradeType.TimeEcho_MultiAttack)       return maxAttacks;  // 3
    return 0;
}

public float GetPercentOfDamageHealed()   // 只有精灵形态治疗
{
    if (ShouldBeWisp() == false) return 0;
    return damagePercentHealed;   // 0.3
}
```

> **讲解点**：用 `upgradeType` 做能力开关，而不是给每个分支建一个子类。因为"时间回响"这个技能的**核心流程是相同的**（生成残影 → 残影模仿玩家动作 → 到时间消失），只是参数和收尾行为不同。给攻击型和精灵型各建一个子类会导致 90% 代码重复。

**模式二：数值随等级线性成长（属性表达式）**

`Skill_DomainExpansion.cs`：

```csharp
[SerializeField] private float baseMaxDomainSize = 10;
[SerializeField] private float baseExpandSpeed = 3;
[SerializeField] private float baseDuration = 5;
[SerializeField] private float baseSpellsToCast = 10;

// 用只读属性把"基础值 + 等级成长"表达成统一公式
public float maxDomainSize     => baseMaxDomainSize + totalLevel * 1f;
public float expandSpeed       => baseExpandSpeed + totalLevel * 0.5f;
public float slowDownDomainDuration => baseDuration + totalLevel * 0.5f;
public float spellCastingDomainDuration => baseDuration + totalLevel * 0.5f;
public float spellsToCast      => baseSpellsToCast + totalLevel * 2f;
```

> **讲解点**：`totalLevel` 是**跨分支累计**的等级（不管选了哪条分支都算）。所以一个技能点得越多，领域越大、法术越多，和分支选择解耦。这比"每个分支各自写一套成长曲线"更好维护。

**模式三：跨技能调用（组合而非继承）**

这是整个项目最精彩的一处。`Skill_DomainExpansion.CastSpell()`：

```csharp
private void CastSpell(Transform target)
{
    // 回响领域：领域内周期性生成"时间回响"残影
    if (upgradeType == SkillUpgradeType.Domain_EchoSpam)
    {
        Vector3 offset = Random.value < 0.5f ? new Vector2(-1, 0) : new Vector2(1, 0);
        skillManager.timeEcho.CreateTimeEcho(target.position + offset);   // ← 调用另一个技能
    }

    // 碎片领域：领域内周期性生成追踪碎片
    if (upgradeType == SkillUpgradeType.Domain_ShardSpam)
    {
        skillManager.shard.CreateRawShard(target, true);                  // ← 调用另一个技能
    }
}
```

而 `Skill_TimeEcho` 暴露的正是这两个方法：

```csharp
public void CreateTimeEcho(Vector3? targetPosition = null)
{
    Vector3 position = targetPosition ?? transform.position;
    GameObject timeEcho = Instantiate(timeEchoPrefab, position, Quaternion.identity);
    timeEcho.GetComponent<SkillObject_TimeEcho>().SetupEcho(this);
}
```

领域目标池用随机取样 + 惰性清理：

```csharp
private List<Enemy> trappedTargets = new List<Enemy>();

private Transform FindTargetInDomain()
{
    if (trappedTargets.Count == 0) return null;
    int randomIndex = Random.Range(0, trappedTargets.Count);
    Transform target = trappedTargets[randomIndex].transform;
    if (target == null)                  // Unity 对象被销毁后 == null 为 true
    {
        trappedTargets.RemoveAt(randomIndex);   // 惰性清理，避免每帧遍历整个列表
        return null;
    }
    return target;
}
```

> **面试讲解点（强烈建议重点讲这段）**：
> 领域展开有三种分支，其中两种是"召唤型"：回响领域每 0.5 秒在领域内随机敌人身上生成一个时间回响残影，碎片领域则生成追踪碎片。
>
> 关键设计是：**领域技能不重新实现残影和碎片，而是直接调用已经存在的技能组件。** `Skill_DomainExpansion` 通过 `skillManager.timeEcho` / `skillManager.shard` 拿到 `Skill_TimeEcho` 和 `Skill_Shard` 的引用，调用它们暴露的 `CreateTimeEcho()` / `CreateRawShard()`。
>
> 这样做的价值：**技能之间形成组合而不是复制**。如果以后我给时间回响加了新的升级分支，领域召唤出来的残影会自动继承新行为，不需要改领域代码。
>
> 这也体现了 `Player_SkillManager` 作为**聚合根**的价值——它不只是个容器，它是技能之间互相发现的**服务定位点**。

#### 技能对象（SkillObject）的生命周期

技能释放产生的运行时实体走统一的 `SkillObject_Base`：

```
Skill_Base.TryUseSkill()
  → Instantiate(skillObjectPrefab)
  → GetComponent<SkillObject_XXX>().SetupXXX(this)   // 反向注入技能引用
  → SkillObject 自己跑生命周期（AnimationTriggers 驱动伤害判定）
  → 到时/销毁时回调技能（如 ClearTargets）
```

`SkillObject_AnimationTriggers` 与玩家状态机共用同一套"动画事件驱动判定"的模式，保持一致。

#### 数据驱动的部分

- `Skill_DataSo`（ScriptableObject）：35 个技能数据资产，按目录分组（`Skill_Data_Dash` 6 个、`Skill_Data_TimeEcho` 7 个、`Skill_Data_Shard` 5 个、`Skill_Data_Domain` 3 个等）
- `SkillDataManager`：Resources 加载 → 建映射 → 提供 `GetSkillType(upgradeType)` / `GetCurrentLevel` / `GetLevelData` 查询
- `SkillTree` UI：节点 + 连线 + 前置/冲突校验 + 拖拽绑定

#### 已知问题（面试被问到要主动认）

- `SkillSlotManager.HandleSkillSlotInput()` 用的是裸 `Input.GetKeyDown(GetSlotKey(i))`（`SkillSlotManager.cs:99`），**绕过了 `GameInput` 的 UI 阻塞链**。注意它取键位是走 `GameInput.GetBinding(...)` 的（支持重绑），但**查询动作本身没走 `GameInput.GetKeyDown`**，所以不经过 `IsGameBlocked`。结果：UI 面板打开（`timeScale = 0`）时技能仍可释放；聊天打字时 H/Y/U/I/O 会误触发技能（Y 还是聊天开关）
- `GameInput.cs` 中 `SkillSlot5` 与 `DomainExpansion` **都绑定了 O 键**，按 O 会同时触发

**另外几处我自己审查出来的设计不一致（面试可主动讲，都是"没重构"而非"不会写"）：**

| 问题 | 位置 | 说明 |
|------|------|------|
| **升级分支互斥语义不一致** | `Skill_TimeEcho` vs `Skill_Shard` | `Skill_TimeEcho` 用 `upgradeType == X` 等值判断，7 个分支**只有一个能生效**；而 `Skill_Shard.TryUseSkill()` 用 5 个 `if (Unlocked(...))` 顺序执行，**允许共存**。同一套架构里两套语义并存 |
| **`totalLevel++` 语义污染** | `Skill_Base.cs:36` | `SetSkillLevelData` 每次调用无条件自增，但它在"升级"和"数据刷新"（`SkillSlotManager.OnSkillDataUpdated`）两种场景都会被调用，**读档/重复同步会重复累加**，导致领域尺寸、附魔上限虚增 |
| **AOE 暴击被独立随机 N 次** | `SkillObject_Base.DamageEnemiesInRadius` | `GetAttackData()` 在 `foreach` **循环体内**调用，打 5 个敌人就 `new AttackData` + `new ElementalEffectData` 各 5 次，且每个目标独立 roll 暴击。既是 GC 热点，也是语义争议（AOE 应共享一次暴击判定） |
| **元素抗性算了但没用** | `Entity_StatusHandler.cs:90-93` | `float finalDuration = duration * (1 - iceResistance);` 算出来了，但第 93 行 `StartCoroutine(ChillCoEffecteCo(duration, ...))` 传的是**原始 `duration`**。`finalDuration` 是死变量，**冰抗的减速时长减免完全未生效** |
| **零对象池** | 技能对象 / 残影 / VFX | 全部 `Instantiate` + `Destroy`。残影间隔最低 `0.04s`，**一次 1 秒冲刺最多生成 25 个带 `SpriteRenderer` 的对象**。全工程 grep `ObjectPool` 无命中 |
| **派发层硬编码** | `Player_SkillManager.cs:59-80` | `GetSkillByType` 是 12 分支 `switch`，`Awake` 里 12 行手工 `GetComponentInChildren`。**行为多态，但注册/派发不是多态** |

> **面试话术**："技能系统的行为层是真正多态的——`TryUseSkill()` 是 virtual，运行时 vtable 派发。但注册和派发层我没重构完，`GetSkillByType` 还是硬编码 switch。改进方向很明确：用 `GetComponentsInChildren<Skill_Base>()` 一次抓取，按 `skillType` 字段自动建 `Dictionary<SkillType, Skill_Base>`。另外对象池和 `AOE` 暴击判定这两处也是我审查时列出的性能重构点。"
>
> **主动说出这三条，比被面试官翻代码发现要好得多。** 而且这三条改进方向都能当场讲清实现方案。

> **主动认领的讲法**："这两个是我在 2026-08-14 的全系统代码审查里发现的。技能槽输入应该走 `GameInput.GetKeyDown(Action.SkillSlot1..5)` 复用阻塞链，而不是直接读 `Input`。我已经列进了修复计划。"
>
> **这句话的价值远大于隐瞒**。实习生能说出"我知道我项目哪里有问题、为什么是问题、怎么改"，比一个自认为完美的候选人可信得多。

---

### A-6 属性系统：双乘区 Modifier 链

#### Stat 的实现（77 行，但设计完整）

```csharp
[Serializable]
public class Stat
{
    [SerializeField] private float baseValue;
    [SerializeField] private List<StatModifier> modifiers = new List<StatModifier>();
    private float finalValue;

    public float GetValue() { finalValue = GetFinalValue(); return finalValue; }

    public void AddModifier(float value, string source, bool isPercentage = false)
    {
        modifiers.Add(new StatModifier(value, source, isPercentage));
    }

    // 按 source 批量移除（装备卸下时用 source 一次性清掉该装备的所有加成）
    public void RemoveModifier(string source)
    {
        modifiers.RemoveAll(modifier => modifier.source == source);
    }

    // 最终值 = (基础值 + 固定值修饰符之和) × (1 + 百分比修饰符之和)
    // 固定值先加，百分比统一乘——百分比词缀（+8%物伤）才能正确生效
    private float GetFinalValue()
    {
        float finalValue = baseValue;
        float percentageSum = 0f;

        foreach (var modifier in modifiers)
        {
            if (modifier.isPercentage) percentageSum += modifier.value;  // 百分比：累加乘区
            else                       finalValue += modifier.value;     // 固定值：直接加
        }
        return finalValue * (1f + percentageSum);
    }
}

[Serializable]
public class StatModifier
{
    public float value;
    public string source;        // ← 关键：来源标识，用于精确移除
    public bool isPercentage;
}
```

#### 四组属性分类

| 组 | 类型 | 内容 |
|----|------|------|
| `Stat_ResourceGroup` | 资源 | maxHP、healthRegen |
| `Stat_MajorGroup` | 主属性 | strength、agility、intelligence、vitality |
| `Stat_OffenseGroup` | 攻击 | attackSpeed、phyiscalDamage、critChance、critPower、armorReduction、elementalHeart、fire/ice/lightningDamage |
| `Stat_DefenseGroup` | 防御 | armor、evasion、fire/ice/lightningRes |

#### 为什么"固定值先加、百分比后乘"很重要

这是最常见的数值系统错误。假设基础攻击 100，装备 A 给 +50 固定，装备 B 给 +50%：

- **正确**：（100 + 50）× 1.5 = **225**
- **错误**（百分比先算）：100 × 1.5 + 50 = **200**

差 25。更严重的是，如果百分比分散在加法链里，**属性顺序会影响结果**，玩家会发现"先穿 A 再穿 B"和"先穿 B 再穿 A"数值不一样。我的实现把所有百分比聚合到一条乘区，与顺序无关。

#### `source` 字段的价值 —— 装备卸下的正确性

```csharp
// 装上一件装备
stat.AddModifier(+10, source: "Equipment_SwordOfFire");
stat.AddModifier(+0.08f, source: "Equipment_SwordOfFire", isPercentage: true);

// 卸下时一次性清掉该装备的所有加成
stat.RemoveModifier("Equipment_SwordOfFire");   // RemoveAll 匹配 source
```

> **面试讲解点**：如果不用 source 而是记录 modifier 的索引或对象引用，一旦中间有其他来源插入/移除，索引就错位了。用**字符串来源标识 + RemoveAll**，卸装备永远精确且与顺序无关。副作用是同一装备重复装备时需要唯一 source（我用了装备实例 ID）。

#### 已知问题（要主动说）

`GetValue()` **每次调用都完整重算一遍**（遍历 modifiers 列表），没有 dirty flag 缓存。而 `GetMaxHP()` / `GetPhyiscalDamage()` / `GetBaseCritChance()` 这些派生属性内部又各自多次调用 `GetValue()`。在 `Update` 里频繁读属性时会有可观的 CPU 开销。

> **改进方案（面试可以答）**："加一个 `isDirty` 标志，`AddModifier`/`RemoveModifier`/`SetBaseValue` 时置脏，`GetValue()` 只在脏时重算。这是标准的缓存失效模式。"

---

### A-7 敌人 AI 与 Boss 设计

#### 敌人 FSM：阈值驱动的简易 AI

`Enemy_BattleState.Update()`：

```csharp
public override void Update()
{
    base.Update();
    enemy.UpdateAffixes();                         // 驱动精英词缀逐帧行为（光环/AoE）

    RaycastHit2D playerHit = enemy.PlayerDetected();   // 射线检测玩家
    bool hasDetectedPlayer = playerHit;

    if (hasDetectedPlayer) { UpdateTarget(playerHit.transform); UpdateBattleTimer(); }

    if (BattleTimeIsOver()) stateMachine.ChangeState(enemy.idleState);       // 5 秒没看到玩家 → 脱战

    if (WithInAttackRange() && hasDetectedPlayer) stateMachine.ChangeState(enemy.attackState);
    else enemy.SetVelocity(enemy.GetBattleMoveSpeed() * enemy.DirctionToPlayer(), rb.linearVelocity.y);
}

private bool BattleTimeIsOver() => Time.time > lastTimeWasInBattle + enemy.battleTimeDuration;
private bool WithInAttackRange() => DistanceToPlayer() < enemy.attackDistance;
private bool ShouldRetreat() => DistanceToPlayer() < enemy.minRetreatDistance;
```

**脱战机制**：`battleTimeDuration`（默认 5 秒）是"最后一次看到玩家的时间 + 5 秒"。这是个简洁的脱战设计。

**智能随机待机/巡逻**（`Enemy.cs`）——避免"机械感"的小设计：

```csharp
// 1.5~4.5s，偶尔长待机（5% 概率 4~5.5s）增加不可预测性
public float GetRandomIdleTime()
{
    Random.value < 0.05f ? return Random.Range(4f, 5.5f) : ...;
    return Random.Range(1.5f, 4f);
}
```

> **讲解点**：纯均匀随机的巡逻节奏会显得机械。加 5% 概率的长间隔，让玩家偶尔"以为它要动了结果没动"，行为更难预测。

#### 敌人类型系统：策略模式

```
EnemyTypeSystem (基类)
 ├─ NormalEnemyTypeSystem
 ├─ EliteEnemyTypeSystem
 └─ BossEnemyTypeSystem
```

`Enemy` 按 `EnemyType` 枚举激活对应的类型系统，各自提供体型/属性/速度/攻击范围的倍率乘区。

#### Boss：动作池调度器（不是状态机驱动动作）

`Boss_SlimeKing.cs`（752 行，全项目最大的玩法类）的架构注释说得很清楚：

```csharp
// 史莱姆王——动作池调度器 Boss AI
// 出生即冻结 FSM（inert），BeginFight() 才启动动作循环；EntityDead() 停调度器+解冻播死亡
```

**动作池 + 冷却门控**（每个动作有自己的冷却时间和距离条件）：

```csharp
[Header("普通攻击·原地起跳落下")]
[SerializeField] private float closeJumpCooldown = 3f;   // 原地跳冷却
[SerializeField] private float closeJumpRange = 4f;      // 贴身触发距离

[Header("大跳·从远处扑过来")]
[SerializeField] private float bigJumpCooldown = 8f;         // 大跳冷却
[SerializeField] private float bigJumpMinRange = 7f;         // 距玩家超此值才大跳
[SerializeField] private float bigJumpDescentGravityMult = 1.8f;

[Header("冲刺")]
[SerializeField] private float dashCooldown = 5f;
[SerializeField] private float dashRangeMin = 3f;
[SerializeField] private float dashRangeMax = 10f;

[Header("传送攻击")]
[SerializeField] private float teleportRange = 15f;      // 超距触发
[SerializeField] private float farTeleportDelay = 4f;    // 超距持续多久才传送
```

**四阶段状态机**：

```csharp
public enum BossPhase { Normal, Split, Stagger, Stealth }

[Header("分裂")]
[SerializeField] private float splitThreshold = 0.3f;     // 30% 血触发分裂
[SerializeField] private float mainSplitScale = 0.8f;     // 本体变小
[SerializeField] private float childSplitScale = 0.6f;    // 子体更小
[SerializeField] private float splitHpPercent = 0.3f;     // 分裂后各自 30% 血

[Header("濒死隐身·一次性保命")]
[SerializeField] private float stealthThreshold = 0.01f;      // 强制保留 1% 血
[SerializeField] private float stealthHealRate = 0.02f;       // 隐身回血 2%/s
[SerializeField] private float stealthHealCap = 0.29f;        // 封顶 29%（低于分裂线，防止循环）
[SerializeField] private int   stealthMaxSummons = 5;         // 召唤次数上限，防无限刷怪
[SerializeField] private int   stealthMaxAlive = 4;           // 同时存活上限
```

**Boss 最精彩的边界处理**（建议重点讲）：

```csharp
public override void EntityDead()
{
    // 残血未分裂却被一击打到 0（玩家高伤同时跨 30% 分裂 + 致死阈值）→ 先进分裂，
    // 分裂会复活本体+子体各 30%，避免直接进濒死隐身跳过分裂阶段
    if (hasSplit == false)
    {
        health.Revive();                                          // 清除 isDead（死亡流程已置位）
        health.SetCurrentHP(health.GetMaxHP() * splitHpPercent);  // 立即显示 30%
        StartSplit();
        return;
    }

    // 濒死保命：受致命伤害时强制保留 1% 血进入隐身回血+召唤，不死
    if (stealthUsed == false)
    {
        stealthUsed = true;                                       // 一次性
        health.Revive();
        health.SetCurrentHP(health.GetMaxHP() * stealthThreshold);
        StartStealth();
        return;
    }

    isFighting = false;
    // 停调度器 / 停隐身 / 停当前动作 / 解冻 FSM ...
}
```

> **面试讲解点（这段最能体现"想过边界情况"）**：
> Boss 有两道阈值：30% 血分裂、0% 血触发一次濒死保命。看起来没问题——但**如果玩家伤害足够高，一击从 35% 打到 0%，会同时跨过两条线**。如果按顺序处理，Boss 会直接跳过分裂阶段进隐身，玩家永远看不到第二阶段。
>
> 我的处理是：在 `EntityDead()` 里先检查 `hasSplit`。**没分裂过的话，强制把死亡逆转成分裂**——复活、设血为 30%、走分裂流程。这样无论玩家伤害多高，分裂阶段一定会出现。
>
> 另外几个防御：
> - 濒死保命的回血**封顶在 29%**，低于 30% 的分裂线。否则 Boss 隐身回血超过 30% 又触发分裂，会形成循环。
> - 隐身召唤有**总次数上限（5）和同时存活上限（4）**，防止无限刷怪把玩家卡死。
> - `stealthUsed` 是一次性标志，保命只触发一次，不会无限续命。

**Boss 的接触伤害走标准管线**（不另起一套）：

```csharp
// 身体 Trigger 接触玩家 → 接触即伤害（所有攻击命中都靠自身碰撞体；玩家无敌帧控频）
private void OnTriggerStay2D(Collider2D other)
{
    if (contactEnabled == false) return;                    // 落地后摇期间关闭
    if (other.CompareTag("Player") == false) return;
    combat?.DealDamageTo(other, contactDamageMult * activeContactMult);  // 走标准伤害管道，攻击倍率生效
}
```

> **讲解点**：Boss 的冲刺攻击（1.5x 伤害）、传送砸落（2x 伤害）不是另写一套伤害逻辑，而是临时改 `activeContactMult` 后走同一个 `DealDamageTo` 管道。**一套伤害公式，Boss 只是改了倍率。** 这样护甲/抗性/元素/暴击全部自动生效，不会出现"Boss 的伤害不吃玩家护甲"这种经典 bug。

#### 精英词缀系统：接口 + 生命周期

`IEnemyAffix` 接口（注释已乱码，但代码结构清晰）：

```csharp
public interface IEnemyAffix
{
    string AffixId { get; }        // 唯一标识（如 "affix_fire_aura"），与 AffixDatabase 对应
    string DisplayName { get; }    // UI 显示名
    AffixTier Tier { get; }        // 词缀等级，影响掉落稀有度加成和选取权重

    void OnApplied(Enemy enemy);      // 生成时：注入 Stats Modifier、注册 VFX、启动协程
    void OnRemoved(Enemy enemy);      // 死亡/Destroy 时：还原 Stats、停止协程、清理 VFX
    void OnBattleUpdate(Enemy enemy); // 每帧 BattleState 调用：光环判定、AoE、召唤检测
    float GetLootBonus();             // 掉落稀有度加成（Common=0.05, Rare=0.10, Legendary=0.20）
    string GetTooltipText();          // 玩家看到的词缀说明文本
}
```

**18 个词缀实现**，分两类：

- **数值类（继承 `StatAffixBase`，3 个）**：`StoneSkinAffix`（石肤）、`ThickHideAffix`（厚皮）、`BerserkAffix`（狂暴）
- **行为类（直接实现接口，15 个）**：`FireAuraAffix`（火焰光环）、`IceNovaAffix`（冰霜新星）、`FlameTrailAffix`（火焰轨迹）、`SlowAuraAffix`（减速光环）、`VulnerabilityHexAffix`（易伤诅咒）、`HealingCurseAffix`（治疗诅咒）、`VampiricStrikeAffix`（吸血打击）、`EnrageTimerAffix`（狂暴计时）、`ReactiveArmorAffix`（反应护甲）、`MirrorCloneAffix`（镜像分身）、`SelfDestructAffix`（自爆）、`BarrierShieldAffix`（护盾）、`LightningConduitAffix`（闪电导体）

**数据驱动的事件钩子**（`Enemy.cs`）：

```csharp
// 词缀系统伤害事件 — 行为类词缀通过订阅这些事件响应战斗
public event System.Action<float> OnEnemyDealtDamage;   // 敌人造成伤害时（吸血、闪电导体订阅）
public event System.Action<float> OnEnemyTookDamage;    // 敌人受到伤害时（反应护甲订阅）

public void ReportDealtDamage(float totalDamage) => OnEnemyDealtDamage?.Invoke(totalDamage);
public void ReportTookDamage(float totalDamage)   => OnEnemyTookDamage?.Invoke(totalDamage);
```

调用点在标准管道里（`Entity_Combat.ProcessAttackOnTarget` 和 `Entity_Health.TakeDamage`），所以**词缀不需要 hook 任何特殊路径**。

> **面试讲解点**：词缀系统用了**接口 + 生命周期钩子 + 事件订阅**三层解耦。
> 1. 接口定义"一个词缀必须做什么"，`StatAffixBase` 为纯数值词缀提供默认实现，避免简单词缀写一堆空方法。
> 2. 伤害事件由标准战斗管道触发，词缀订阅即可。**新增词缀不需要改战斗代码**——这是开闭原则的实际落地。
> 3. `AffixSpawner` + `EliteAffixDatabase` 按敌人层级和权重组装词缀组合，完全数据驱动（策划配表即可，见 ADR-0001/ADR-0002）。

---

### A-8 数据驱动的词缀与掉落生成

#### 稀有度加权随机（`RarityCalculator.cs`）

```csharp
private static readonly float[] RarityMultipliers = { 1.0f, 1.5f, 2.0f, 2.5f, 3.0f };  // 普通→传说

public static LootRarity GetVariedRarity(LootRarity baseRarity, int maxSteps, float rarityBonus)
{
    if (maxSteps <= 0) return baseRarity;

    int baseIndex = (int)baseRarity;
    int minIndex = Mathf.Max(0, baseIndex - maxSteps);
    int maxIndex = Mathf.Min((int)LootRarity.传说, baseIndex + maxSteps);

    int range = maxIndex - minIndex;
    int[] weights = new int[range + 1];

    for (int i = 0; i <= range; i++)
    {
        int distance = i - (baseIndex - minIndex);   // 距基准稀有度的步数

        switch (distance)
        {
            case 0:  weights[i] = 100; break;                                          // 基准：100 权重
            case 1:  weights[i] = 15 + Mathf.RoundToInt(rarityBonus * 0.75f); break;   // +1 步
            case 2:  weights[i] = 3  + Mathf.RoundToInt(rarityBonus * 0.5f);  break;   // +2 步
            case 3:  weights[i] = 1  + Mathf.RoundToInt(rarityBonus * 0.25f); break;   // +3 步
            case -1: weights[i] = 15; break;                                           // 向下浮动不吃加成
            case -2: weights[i] = 3;  break;
            case -3: weights[i] = 1;  break;
            default: weights[i] = 1;  break;
        }
    }

    // 加权随机
    int totalWeight = 0;
    for (int i = 0; i < weights.Length; i++) totalWeight += weights[i];

    int randomWeight = UnityEngine.Random.Range(0, totalWeight);
    int currentWeight = 0;
    for (int i = 0; i < weights.Length; i++)
    {
        currentWeight += weights[i];
        if (randomWeight < currentWeight) return (LootRarity)(minIndex + i);
    }
    return baseRarity;
}
```

> **面试讲解点**：
> 1. **基准 100，上下浮动一步仅 15**。也就是 100/(100+15+15) ≈ 77% 的装备保持基准稀有度。这个"基准占绝对大头"的设计保证掉落可预测——策划配一张"普通怪掉普通装"的表，实际产出不会大面积偏离。
> 2. **稀有度加成只作用于向上浮动**。`case 1/2/3` 加 `rarityBonus`，`case -1/-2/-3` 不加。因为"幸运值"在玩家认知里只应该带来好处，如果它同时提高向下浮动的概率，玩家会觉得幸运值反而有害。这是**数值设计要服从玩家直觉**的例子。
> 3. **加成的步长递减**（×0.75 / ×0.5 / ×0.25）。让幸运值主要提升"升一档"的概率，而不是让"跳三档"变得常见——跳档过多会让稀有度失去意义。
> 4. **可核算的具体数字**（面试可以现场算，很有说服力）：
>    - 普通底材（`maxRaritySteps = 1`）+ 无词缀普通怪（bonus = 50）：权重 100（普通）与 `15 + Round(37.5) = 53`（精良）→ **P(升为精良) = 53/153 ≈ 34.6%**
>    - 同一件普通底材 + 3 词缀传说精英（bonus = 50 + 60 = 110）：`15 + Round(82.5) = 97` → **P(升为精良) = 97/197 ≈ 49.2%**
>    - 且 `maxSteps = 1` 时 `maxIndex` 被夹到 1，所以**普通底材永远不会被直接抬成稀有**
>
> 讲这个算例的价值在于：它同时展示了"权重表设计"和"精英词缀 → 掉落品质"的**可见因果链**——玩家打传说精英，掉落品质提升是能算出来的，不是黑箱。

> **小提醒（面试若被追问）**：`Mathf.RoundToInt` 用的是"四舍六入五取偶"（banker's rounding），所以 37.5 → 38、82.5 → 82。这个细节如果被问到能答出来，会显得你真的跑过数字。

#### 装备词缀程序化生成（`EquipmentAffixGenerator.cs`，274 行）

**稀有度调性表**（这个设计很有想法）：

```csharp
// 稀有度 → 允许的词缀类型集合（稀有度调性表）
// 普通=正/双/负（赌博）  优秀=正/双  稀有=全双刃（温和代价）  史诗=全正面  传说=双刃+正面
private static AffixType[] GetAllowedAffixTypes(LootRarity rarity)
{
    return rarity switch
    {
        LootRarity.普通 => new[] { AffixType.Positive, AffixType.DoubleEdge, AffixType.Negative },
        LootRarity.精良 => new[] { AffixType.Positive, AffixType.DoubleEdge },
        LootRarity.稀有 => new[] { AffixType.DoubleEdge },                          // 强制全双刃
        LootRarity.史诗 => new[] { AffixType.Positive },                            // 强制全正面
        LootRarity.传说 => new[] { AffixType.DoubleEdge, AffixType.Positive },
        _ => new[] { AffixType.Positive }
    };
}
```

**词缀数量与"空词缀"概率**：

```csharp
// 每个稀有度都有概率完全不带词缀（普通60%、精良50%、稀有40%、史诗30%、传说20%）
private static int GetMaxAffixCount(LootRarity rarity)
{
    float noAffixChance = rarity switch
    {
        LootRarity.普通 => 0.6f, LootRarity.精良 => 0.5f, LootRarity.稀有 => 0.4f,
        LootRarity.史诗 => 0.3f, LootRarity.传说 => 0.2f, _ => 0.5f
    };
    if (Random.value < noAffixChance) return 0;

    return rarity switch
    {
        LootRarity.普通 => 1,
        LootRarity.精良 => Random.Range(1, 3),   // 1-2
        LootRarity.稀有 => Random.Range(2, 4),   // 2-3
        LootRarity.史诗 => Random.Range(3, 5),   // 3-4
        LootRarity.传说 => 4,                    // 固定 4
        _ => 0
    };
}
```

**Tier 保底机制**：

```csharp
// 保底机制：词缀 tier 下限 = 稀有度-1 级（普通装保底普通，传说装保底史诗），与上限同为稀有度级
int floorTier = Mathf.Max(0, (int)rarity - 1);
int maxTier   = (int)rarity;

var selected = AffixSelector.Select(
    pool, count, maxTier,
    getId: e => e.affixId, getTier: e => (int)e.tier, getWeight: e => e.weight,
    minTier: floorTier);
```

**每段独立 roll（支持混装正负效果）**：

```csharp
for (int i = 0; i < entry.statModifiers.Length; i++)
{
    AffixStatModifier sm = entry.statModifiers[i];

    // 该段独立 roll：负值段（min < 0）roll 出负数 = 负面效果
    float rawValue = Random.Range(sm.minValue, sm.maxValue);

    // 含小数的范围保留一位小数，整数范围取整（避免 0.3237033 这种难看的显示）
    bool hasDecimalRange = Mathf.Abs(sm.minValue - Mathf.Round(sm.minValue)) > 0.001f ||
                           Mathf.Abs(sm.maxValue - Mathf.Round(sm.maxValue)) > 0.001f;
    float value = hasDecimalRange ? Mathf.Round(rawValue * 10f) / 10f : Mathf.Round(rawValue);

    // 百分比词缀存为小数（8% → 0.08）
    if (sm.isPercentage) value = value / 100f;
    // ...
}
```

> **面试讲解点**：
> 1. **稀有度调性表**是"用代价换强度"的落地。史诗装强制只出正面词缀（稳定强），稀有装强制全双刃（收益高但有代价），传说装是两者混合。这样不同稀有度不只是数值高低，而是**玩法手感不同**。
> 2. **每个稀有度都有"完全不带词缀"的概率**（普通 60% → 传说 20%）。如果没有这个概率，普通装永远带 1 条词缀，玩家对"白板装"就没有期待感。留出空档后，掉到带词缀的装备才有惊喜。
> 3. **保底 tier**：传说装最低也出史诗级词缀。避免"打到传说装结果词缀是普通级"的挫败。这是标准的**下限保护**设计。
> 4. **每段独立 roll** 让一条词缀可以同时有正面和负面段（比如"攻击 +12，生命 -20"），比"整体是好词缀或坏词缀"更丰富。

#### 已知问题（主动说）

审查报告记录了 `rarityMultiplier` 存在**双重应用风险**：`RarityCalculator.GetModifiedValue()` 会在物品属性上乘一次差值倍率，而 `LootManager`/`EquipmentSystem` 侧可能再乘一次。这个我在审查里列为"待验证（W-3）"。

---

### A-8b 铁匠三件套：制作 / 分解 / 合成

三个子系统在架构上高度统一：都是**静态无状态逻辑类 + `GetXxxFailReason` 校验模式 + `TryXxx` 执行模式**。UI 层只做展示与按钮禁用。

#### 校验与执行共用同一真源（防 UI/逻辑不一致）

```csharp
// CraftingSystem：CanCraft 就是 GetCraftFailReason(...) == null
// 返回的中文原因串直接当 UI 按钮文案用（UI_CraftPanel.RefreshDetail）
public static string GetCraftFailReason(CraftingRecipe recipe, PlayerInventorySystem invSys)
{
    // 返回 "配方无效" / "背包未就绪" / "背包已满" / "金币不足" / "材料不足"
}

public static bool CanCraft(CraftingRecipe recipe, PlayerInventorySystem invSys)
    => GetCraftFailReason(recipe, invSys) == null;
```

> **面试讲解点**：校验逻辑只写一次。UI 用 `GetCraftFailReason` 的返回值**直接当按钮禁用原因文案**，所以"按钮为什么灰"和"实际为什么不能做"永远是同一个原因，不可能出现 UI 说能点但点了失败。这是把"人类可读原因"和"布尔校验"合并成一个真源的技巧。

#### 一张配方表双向服务制作与分解

`DismantleSystem.CalculateOutput()` **不读独立的分解配置表**，而是反推制作配方：

```csharp
// 按 resultItem 线性匹配 CraftingRecipeDB，取制作配方
var recipe = FindRecipe(DB, item.itemData);
// 产出 = max(1, round(材料数 × 0.5 × 稀有度倍率))，损耗 50%
int amount = Mathf.Max(1, Mathf.RoundToInt(m.count * 0.5f * rarityMult));
```

> **讲解点**：分解不需要第二张表。策划只维护制作配方，分解自动按 50% 损耗反推。**这消除了一整类"配方表和分解表不同步"的 bug**，也减少了策划工作量。代价是无法为某件装备单独定制分解产出。
>
> ⚠️ 旧类型 `DismantleTable`/`DismantleEntry`/`DismantleMaterial` 是遗留物，全工程零引用（死代码）。

#### 合成的概率透明化

三条独立概率通道，并且 UI 把每一行都算给玩家看：

```csharp
// ① 小成功（稀有度 +1）：按目标稀有度递减 + 素材词缀加成
GetBaseSuccessRate(rarity) = 精良 0.50 / 稀有 0.35 / 史诗 0.20 / 传说 0.10
GetAffixCountBonus        = affixCount × 0.02
GetEffectiveSuccessRate   = min(0.9, base + bonus)

// ② 大成功（换更高一级底材，稀有度不变）
GetBigSuccessRate = min(0.9, count × 0.10)      // 3 件 30% → 9 件 90%

// ③ 突破（大成功时 30% 触发）：用高于当前稀有度的词缀替换最弱词缀
BreakthroughChance = 0.3
```

词缀继承用**「数量保底 + 品质分保底」双轨制**：

```csharp
GetGuaranteeCount(N) = max(1, (N - 1) / 2)          // 3→1、5→2、7→3、9→4
qualityScore         = Σ(tier + 1)
K = min(GetGuaranteeCount(N) + qualityScore / qualityPerKeep, targetCount)
// qualityPerKeep 随目标稀有度递增：精良 5 / 稀有 15 / 史诗 30 / 传说 50
```

> **面试讲解点**：合成是"3~9 件同阶装备换 1 件"，如果概率完全不透明，玩家会觉得在被骗。我把基础成功率、词缀加成、大成功率、保底条数、品质分、突破率**逐行显示在 UI 上**。这是"可解释的随机"——玩家能算清楚自己投入 9 件能拿到什么保底。
>
> 词缀继承的两条保底线：投入件数保证保底条数（3 件保 1 条，9 件保 4 条），素材词缀的 tier 总分保证品质分能额外换条数。这样"投入更多、更好的素材"都有明确回报。

#### 已确认的经济漏洞（面试可当成"数值设计意识"来讲）

分解产出 = `round(配方材料数 × 0.5 × 稀有度倍率)`。以 `CraftingRecipes.csv` 的木剑（3 铜 + 20 金币）为例：

| 稀有度 | 倍率 | 分解产出 | 相对制作成本 |
|--------|------|---------|-------------|
| 普通 | 1.0 | 2 铜 | 亏 |
| 稀有 | 2.0 | 3 铜 | 打平 |
| 史诗 | 2.5 | 4 铜 | **净赚** |
| 传说 | 3.0 | 5 铜 | **净赚** |

> 即"把稀有装备合成抬到史诗后分解"在材料层面是正收益循环，只被金币消耗和合成成功率（史诗→传说仅 10%）部分约束。
>
> **面试话术**："这是我在经济系统设计上留下的一个漏洞——分解产出随稀有度线性放大，但合成成本没有同步放大，导致高稀有度存在套利循环。修法有两种：让分解产出对稀有度做次线性映射（比如 `sqrt`），或者让分解产出稀有度无关的固定回收值。"
>
> **主动指出数值漏洞的价值很高**——它证明你不只是实现系统，还会审视系统的经济闭环。

---

### A-9 存档系统（第二强差异化点）

> **定级修正**：我最初把它归为"有缺口、别演示"。逐文件核对后结论要上调——**存档系统是你项目里第二强的差异化亮点**（第一是 GraphView 编辑器）。它证明的是"对数据持久化与间接层的理解"，而这恰好是面试高频考点。**要演示，但限定范围。**

#### 核心技术难点：JsonUtility 不能序列化 SO 引用

这是整个存档系统的**存在理由**，也是最值得讲的一点：

```csharp
// JsonUtility 只能序列化纯数据。把 ItemDataSo 这类 UnityEngine.Object 引用写进 JSON
// 是不可靠的（instanceID 每次运行都变，GUID 又要额外的映射表）。
// 所以存档只写稳定的业务 ID 字符串，读档时反查资产重建。
data.inventory[i] = new InventorySlotData { itemId = "...", ... };
```

`ItemLookup` 就是为这个问题存在的：

```csharp
// Initialize: Resources.LoadAll<ItemDataSo>("Data/ItemData") → 建 itemId → ItemDataSo 字典
public static ItemDataSo Find(string itemId)   // O(1) 反查
```

> **面试讲解点（这是本系统最值钱的一句话）**：
> "Unity 的 `JsonUtility` 不能序列化 `ScriptableObject` 引用。如果直接存对象引用，重新编译或资产移动后引用就断了；存 `GetInstanceID()` 更不行，因为每次运行都会变。
>
> 我的方案是**存档只存稳定的业务 ID 字符串**，读档时通过 `ItemLookup` 从 `Resources` 反查还原。这样存档与资产**解耦**——资产改名、移动目录、甚至重新导入都不影响旧存档。
>
> 代价是所有 `ItemDataSo` 必须在 `Resources` 下，会全量常驻内存、没法按需卸载。如果项目规模再大，我会改用 Addressables 的 `AssetReference` 加一张显式的 ID→地址映射表。"

#### 恢复顺序有真实依赖（不是随便排的）

```
玩家位置
 → ApplyPlayerData（货币/等级/经验/技能点）
 → ApplyStatData（baseValue）
 → ApplySkillData      ← 必须先于背包
 → ApplyInventory
 → ApplyWarehouse
 → ApplyEquipment
 → ApplyQuestData → WorldState.LoadFromSave
 → 收尾：SetCurrentHP(GetMaxHP()) 统一满血
        → Revive()
        → RemoveAllNegativeEffects()   ← 玩家是常驻对象，重载场景不会清运行时状态
        → ClearAllBuffers()
```

> **面试讲解点**：顺序里有两条真实依赖，我在代码里都写了注释：
> 1. **技能必须先于背包**——背包容量由被动技能"背包扩容"决定。如果先按默认容量建背包，再恢复技能，被动扩容就晚了，会出现"无效槽位索引"。
> 2. **满血必须放最后**——如果先设满血再恢复属性，`currentHP` 会被旧的上限 `Clamp` 掉。
>
> 另外收尾要清负面状态和输入缓冲：玩家是 `DontDestroyOnLoad` 的常驻对象，场景重载不会重置它的运行时状态，不清就会出现"读档后还带着上一局的减速"或"缓冲里卡着一条输入"。

#### 程序化内容的存档往返（很多 demo 直接放弃）

装备词缀是随机生成的，但读档不能重随机（否则 SL 大法能刷装备）：

```csharp
// 读档时传入 savedAffixes 按存档精确恢复（不重新随机）
new Inventory_Item(looted, Inventory_Item.FromSaveData(slot.affixes))
// 序列化内容：稀有度 + 稀有度倍率 + 逐条词缀（含负值段）+ 精确槽位
```

> **演示方式（这一段很有说服力）**：捡一件带 4 条词缀的传说装备 → 记住词缀名字和数值 → 存档 → 读档 → Tooltip 里四条词缀**完全一致**。然后用资源管理器直接打开 `%persistentDataPath%\slot_0.json`，展示 pretty-print 的可读 JSON。

#### 场景/常驻对象生命周期

- 玩家是**唯一常驻对象**：`PlayerSpawner` 首次进游戏场景时 `Instantiate` + `DontDestroyOnLoad`，主菜单场景 `SetActive(false)`，每次 `sceneLoaded` 重绑 Cinemachine 虚拟相机并把玩家放到目标传送门 → `isEntryPoint` 存档点 → 第一个存档点（三级兜底）
- 读档跨场景：场景不同 → `pendingLoad` + `SceneManager.sceneLoaded` 回调 + `SceneTransitionFader` 过场；加载后**轮询等待**各单例就绪（5s 超时）+ 强制等一帧（保证场景内 `Start()` 里订阅 UI 的组件已完成订阅）
- `LoadWithReload`：死亡后强制重载存档所在场景（跨场景死亡也能回到存档场景）。场景重载天然重置敌人/宝箱/机关——设计文档明确列为"不保存"的取舍

> **讲解点**："轮询 + 超时 + 强制等一帧"这段是手工调时序的代码，注释里诚实记录了原因。**它同时也暴露了架构缺口**——项目缺少"就绪协议 / 初始化顺序契约"。这句话主动说出来，比等面试官指出更好。

#### 已确认的覆盖面与健壮性缺口

| 缺口 | 后果 |
|------|------|
| **传送门激活状态不持久化** / **Boss 击败状态不持久化**（`BossEncounter.defeatedFlag` 仅运行时内存标志） | 读档后传送门锁回去、Boss 重生 |
| **`lastCheckpointId` 是死数据** | 写入了也读回了，但**全库没有任何地方读它决定重生**（重生实际用存档里的玩家坐标 + `LoadWithReload`） |
| **无异常处理** | `File.WriteAllText` / `File.ReadAllText` / `JsonUtility.FromJson` 全无 try/catch → 存档损坏时抛异常中断读档 |
| **非原子写**（无 temp+rename） | 写入过程中崩溃即坏档 |
| **无备份轮转 / 无校验和 / 无加密** | 明文 JSON，可随手改数值作弊 |
| **版本迁移未实现** | `version >` 拒绝、`version <` 只 `DebugWarning("将自动升级")`，**没有任何迁移代码** |
| **槽位无范围校验** | `MAX_SLOTS = 4` 实际未被使用，`Save(99)` 会写 `slot_99.json` |
| `ApplyEquipment` 忽略 `TryEquipItemToSlot` 失败返回值 | 装备可能静默丢失 |
| `facingDir` 未持久化 | 读档后玩家朝右 |

> **视频建议**：**可以演示存档往返**（装备词缀/技能/任务还原是好的），**不要演示读档后的 Boss 或传送门状态**。
>
> **面试话术**："存档的数据收集和 ID 间接层我做得比较完整，包括程序化词缀的精确还原和恢复顺序的依赖处理。但按生产标准它不算 robust——没有原子写入、没有备份轮转、没有版本迁移代码、没有异常处理、明文可篡改。这几条我在自己的审查报告里都列了。"

---

### A-9b UI 架构（有真正设计好的部分，别自我贬低）

UI 是全项目代码量最大的模块（7,776 行），质量**不均**——有几处设计得很好，也有明显复制粘贴。面试要讲对的那些。

#### 真正的亮点：ModalStack（全项目设计最好的一块）

```csharp
// UI\UI\ModalStack.cs —— 57 行，静态字符串 LIFO 栈
ModalStack.Push("npc_menu");
ModalStack.Pop("npc_menu");      // 仅当 id 在栈顶才弹（防乱序 Pop）
ModalStack.Top                   // Escape 按栈顶分发
ModalStack.IsAnyModalOpen        // 输入阻塞链的唯一事实源
```

配合输入阻塞链：

```csharp
// GameInput.cs:66 —— 一点阻塞，全项目共用
IsGameBlocked => UI_Chat.IsChatFocused || UIManager.IsAnyPanelOpen || IsPlayerControlBlocked;
```

> **面试讲解点**：模态面板管理最容易出的 bug 是"栈乱序"——开商店→开背包→关背包，结果商店也被关了，或者 `timeScale` 被错误复位。我用一个 57 行的字符串 LIFO 栈解决：`Pop(id)` 只在 id 是栈顶时才弹，所以关一个面板不会误关它下面的。Escape 键也按栈顶分发，永远只关最上层。
>
> 输入阻塞是同一个思路——`IsGameBlocked` 是**唯一的阻塞判据**，聊天聚焦、任意面板打开都在这一处收敛。需要阻塞输入的地方不用各自判断。

#### 其他值得讲的实现

| 实现 | 位置 | 亮点 |
|------|------|------|
| **`PanelSwitcher` 真通用组件** | `UI\UI\PanelSwitcher.cs` | `PanelEntry[]` + 索引 + 三种切换动画 + `SetUpdate(true)`，被主面板/技能树/设置三处复用，是**真正的复用**而非复制 |
| **`SceneTransitionFader` 的异步加载门控** | `Others\SceneTransitionFader.cs` | `LoadSceneAsync` + `allowSceneActivation = false` + `progress >= 0.9` 才放行 → **把场景激活的卡顿藏在黑幕下**。全程 `WaitForSecondsRealtime` + tween `SetUpdate(true)`，`timeScale = 0` 也能过场 |
| **`ShopSystem` 是纯业务类** | `UI\UI_Shop\ShopSystem.cs` | **零 UI 依赖** + 事件 `OnDataChanged`。是全项目分层最干净的一处，UI 只订阅不反向驱动 |
| **`WarehouseSystem.Transfer` 有事务意识** | `Others\ItemSystem\WarehouseSystem.cs` | 先预检 → 移除 → **失败回滚**（`source.itemDictionary[sourceSlot] = item` 兜底）；`DepositFromEquipment` 失败时**重新装备回去** |
| **`BaseTip` 有真实共享行为** | `UI\UI_Tip\BaseTip.cs` | 淡入/滑入/抖动/队列在基类，子类只做字符串包装 |
| **DOTween `SetUpdate(true)` 全项目一致** | 各面板 | `timeScale = 0` 时面板动画照播——这是个容易漏的细节，项目里是一致执行的 |
| **`ISkillDropTarget` 接口设计正确** | `Interface\ISkillDropTarget.cs` | `OnSkillDropped(upgradeType, skillData, sourceNode, sourceSlot)` **显式传来源**，不需要实现类回读拖拽单例 |

> **对比说明（面试可讲设计判断）**：技能拖拽接口 `ISkillDropTarget` 显式传 `source`，而物品拖拽接口 `IItemDropTarget` 不传，导致物品落点实现必须回读 `UI_ItemDragHandler.Instance.IsDraggingFromEquipment` 这种全局状态——**这是接口设计上的退步**，我在审查里记了。两个接口在同一个项目里，正好可以对比"显式依赖"和"隐式耦合"的差别。

#### UI 层的已知问题（主动认领）

| 问题 | 说明 |
|------|------|
| **拖拽 Handler 逐行复制** | `UI_SkillDragHandler` 是 `UI_ItemDragHandler` 的近似逐行复制，且 `PerformDropDetection` **从不调用 `CanAcceptSkill`**——接口契约被绕过（只用于悬停染色） |
| **玩家槽位托管逻辑三份复制** | `UI_ShopPanel` / `UI_BlacksmithPanel` / `UI_WarehousePanel` 各自近乎逐字实现"移入/移回 + 订阅 + 隐藏干扰对象" |
| **数据绑定范式不统一** | `UI_InGame`/`UI_QuestTracker`/`UI_BossHealthBar` 是事件驱动，而 `HealthBar`/`UI_AttributeManager`/`UI_AttributeButton`/`UI_StatSlot` 是每帧轮询。**同一仓库里 `UI_PlayerStats` 已订阅 `OnAttributesChanged`，`UI_AttributeManager` 却轮询同一数据**——这是最容易被面试官抓的不一致 |
| **状态查询口径混用** | `IsShopOpen` 用 `activeInHierarchy`，`IsPauseMenuOpen`/`IsWarehouseOpen` 用 `ModalStack.Top`——同一个栈两种查询方式 |
| **`UI_DeathScreen`/`UI_Chat` 不进 ModalStack** | 尽管 `ModalStack.cs` 注释把它们列为模态。死亡界面自己写 `Time.timeScale` → "统一管时间"被破例 |
| **`HideAllToolTips` 必须认识 5 个具体提示类** | 没有 `IToolTip` 接口，加一类提示就要改这个方法 |
| **`UI_SkillToolTip.GetColoredText` 用 `new` 遮蔽基类方法** | 多态实际不存在，基类引用调用会拿到基类实现。两套提示体系（`BaseTip` 动画体系 vs `UI_ToolTip` 定位体系）子类全是**新增重载**而非 override |
| **`UI_Chat.AddMessage` 未转义富文本** | 把 sender/content 直接拼进 TMP 富文本 → **富文本注入面**（玩家输入 `<color=...>` 之类会生效）。另外 `UI_Chat` 用遗留 `Input` 读 `KeyCode.Y`/`Return`，**聊天键无法重映射** |

> **面试话术**："UI 层是我代码量最大也最不均匀的部分。设计得好的地方是 `ModalStack` 的模态管理和输入阻塞收敛、`PanelSwitcher` 的真复用、以及过场的异步加载门控。做得不好的地方是复制粘贴——技能拖拽 Handler 逐行复制了物品拖拽 Handler，玩家槽位托管逻辑在三处重复。还有一个范式不统一的问题：有的面板事件驱动、有的每帧轮询，甚至同一个数据既被订阅又被轮询。这些我都记在审查报告里，改进方向是把拖拽抽象成带 source 的泛型基类、把槽位托管提成共享组件、统一成事件驱动。"

---

### A-10 任务系统与自研 GraphView 编辑器

#### 任务数据模型

`QuestData`（ScriptableObject）→ `List<QuestStage>` → `List<ObjectiveConfig>`，三层结构。

**任务生命周期**：可接 → 已接受 → 可领奖 → 已完成（另有失败态）

**触发类型枚举**（`QuestData.cs`）：

```csharp
public enum QuestTrigger
{
    NpcTalk,      // 与 NPC 对话触发
    AutoUnlock,   // 前置满足后自动接取
    ItemPickup,   // 拾取特定物品触发
}

public string triggerNpcId;    // 从哪个 NPC 接取（用于 NpcTalk）
public string triggerItemId;   // 拾取哪个物品触发（用于 ItemPickup）
```

> ⚠️ **重要提醒**：`AutoUnlock` 和 `ItemPickup` 这两个枚举值目前**只在数据里定义，运行时没有消费方**。`triggerNpcId` 也是零消费。所以视频里不要演示"捡到某物品自动接任务"。
>
> 面试如果被问起任务触发，正确说法是："任务接取目前主要走 NPC 对话。数据模型里我预留了自动接取和拾取触发的入口，但触发分发层还没实现——这是我在代码审查里自己发现并记录的设计缺口。"

#### 事件总线与解耦

`QuestEvents`（静态事件总线）+ `WorldState`（世界状态标志位）：

- `QuestEvents`：任务接受/进度更新/完成/失败的事件广播，任务 UI、NPC 头顶指示器、HUD 追踪器订阅
- `WorldState`：`Set(key, value)` / `Get(key)` 的全局标志位，用于"某事件已发生"的查询

> ⚠️ **已有缺口**：审查报告记录 `WorldState` **只写不读**（死设计），且读档后任务 UI 不同步。这意味着 `WorldState` 当前是个"铺好但没用起来"的基础设施。面试可以说"作为解耦基础设施已就位，但消费方还需要补齐"。

#### QuestTreeEditor —— 自研 GraphView 编辑器（**强有力的加分项**）

这是我认为**对整个求职最有价值的单项**，因为它证明了"能自己造工具"而不只是"会用引擎"。

**技术栈**：`UnityEditor.Experimental.GraphView` + `UnityEngine.UIElements` + `EditorWindow`

**结构**（4 个文件，1118 行）：

| 文件 | 职责 |
|------|------|
| `QuestTreeEditorWindow.cs`（181 行） | `EditorWindow` 主窗口，装配 GraphView + Inspector + Toolbar，MenuItem 入口 |
| `QuestGraphView.cs` | 画布：节点重建、连线、自动布局 |
| `QuestNode.cs` | 图节点：端口、拖拽、视觉状态（前置满足/冲突高亮） |
| `InspectorPanel.cs` | 右侧属性面板：编辑选中节点的任务数据 |

**主窗口实现**（可直接指认）：

```csharp
public class QuestTreeEditorWindow : EditorWindow
{
    private QuestGraphView graphView;
    private InspectorPanel inspectorPanel;
    private List<QuestData> allQuests = new();

    [MenuItem("Tools/任务树编辑器")]
    public static void Open()
    {
        var w = GetWindow<QuestTreeEditorWindow>();
        w.titleContent = new GUIContent("任务树编辑器");
        w.minSize = new Vector2(1000, 600);
        w.Show();
    }

    private void OnEnable() { BuildGraphView(); BuildInspectorPanel(); BuildToolbar(); LoadAllQuests(); }

    private void BuildGraphView()
    {
        graphView = new QuestGraphView(this)
        {
            name = "任务树画板",
            viewDataKey = "QuestTreeGraph"      // ← 利用 GraphView 内建能力：记住缩放/平移位置
        };
        graphView.StretchToParentSize();
        rootVisualElement.Add(graphView);
    }

    // 从资产库加载全部任务，构建图
    private void LoadAllQuests()
    {
        var guids = AssetDatabase.FindAssets("t:QuestData", new[] { "Assets" });
        allQuests.Clear();
        foreach (var guid in guids)
        {
            var path = AssetDatabase.GUIDToAssetPath(guid);
            var quest = AssetDatabase.LoadAssetAtPath<QuestData>(path);
            if (quest != null) allQuests.Add(quest);
        }
        graphView?.RebuildFromQuests(allQuests);
        inspectorPanel?.ClearSelection();
    }

    // 保存：把 GraphView 里的节点位置/连线同步回 ScriptableObject
    private void SaveAllQuests()
    {
        graphView?.ApplyNodeDataToQuests();          // ← 图 → 数据的单向同步

        foreach (var quest in allQuests) EditorUtility.SetDirty(quest);
        AssetDatabase.SaveAssets();
        AssetDatabase.Refresh();
    }

    public void CreateNewQuest()
    {
        var quest = ScriptableObject.CreateInstance<QuestData>();
        quest.questId = "new_quest_" + System.DateTime.Now.Ticks;
        quest.questName = "新任务";
        quest.questType = QuestType.Side;
        quest.stages = new List<QuestStage>();
        quest.stages.Add(new QuestStage { description = "新阶段", objectives = new List<ObjectiveConfig>() });

        string path = EditorUtility.SaveFilePanelInProject("保存任务数据", quest.questName, "asset", "选择保存位置", assetPath);
        if (!string.IsNullOrEmpty(path))
        {
            AssetDatabase.CreateAsset(quest, path);
            AssetDatabase.SaveAssets();
            allQuests.Add(quest);
            graphView?.AddQuestNode(quest);
        }
        else Object.DestroyImmediate(quest);      // ← 用户取消时销毁临时对象，避免内存泄漏
    }
}
```

> **面试讲解点（这段建议这样组织）**：
> "任务系统最初是让我最痛苦的部分——任务之间是**链条关系**（A 完成后解锁 B，B 和 C 冲突）。用 Inspector 一个个点开资产、在字符串字段里填前置任务 ID，很容易填错而且看不出全局结构。
>
> 所以我基于 Unity 的 GraphView 写了一个可视化编辑器。左边是画布，节点代表任务，连线代表前置关系；右边是 Inspector 面板，点节点就能编辑它的阶段和目标。工具栏有保存、重载、新建、自动布局。
>
> 技术上几个点：
> 1. **数据同步是单向的**：`ApplyNodeDataToQuests()` 从图写回 SO。避免双向绑定导致的不一致。
> 2. **用 `AssetDatabase.FindAssets("t:QuestData")` 自动发现全部任务**，不需要手动维护列表。
> 3. **`viewDataKey`** 让 GraphView 自己记住缩放和平移位置，不用我存。
> 4. **新建时如果用户取消保存对话框，要 `DestroyImmediate` 掉临时创建的 SO**，否则会内存泄漏。
>
> 这个工具让我意识到：**策划体验也是工程问题**。"

**这段的价值**：面试官会立刻把你从"会写玩法的学生"归类到"能提升团队效率的工程候选人"。实习岗最稀缺的往往不是写玩法的人，而是能造工具、能减少团队摩擦的人。

---

### A-11 数据管线：xlsx → CSV → ScriptableObject

**为什么需要**：策划不会用 Unity 的 Inspector 批量填 200 件装备。用表格能做批量、能做公式、能版本对比。

**管线实现**（`Assets\Scripts\Editor\CSVImport\`）：

| 文件 | 职责 |
|------|------|
| `CSVImportWindow.cs` | 统一导入窗口（EditorWindow） |
| `CSVHelper.cs`（174 行） | CSV 解析核心（分隔符、引号转义、编码） |
| `ItemCSVImporter.cs`（232 行） | 物品导入 |
| `LootTableCSVImporter.cs` | 掉落表导入 |
| `StatSetupImporter.cs` | 属性配置导入 |
| `CNStatMapping.cs` | **中文表头 → 英文枚举的映射层** |
| `EquipmentAffixCSVImporter.cs`（156 行） | 装备词缀导入 |
| `CraftingRecipeCSVImporter.cs` | 配方导入 |
| `AffixSystemGenerator.cs` | 从数据批量生成词缀资产 |

**`CNStatMapping` 存在的意义**（这是个很实际的工程决策）：

策划用中文表头填表（"力量"、"暴击率"、"火抗"），程序内部用英文枚举（`StatType.Strength`、`StatType.CritChance`、`StatType.FireResistance`）。映射层把两者隔开：

```csharp
// 中文列名 → StatType 枚举，避免策划改一次表头就崩，也避免代码里散落中文字符串
"力量"   → StatType.Strength
"暴击率" → StatType.CritChance
"火抗"   → StatType.FireResistance
```

> **面试讲解点**：
> 1. **映射层是防腐层**。没有它的话，代码里会散落 `if (colName == "力量")` 这样的中文字符串比较，改表头就全崩。
> 2. 数据资产规模：**114 个 ScriptableObject**，覆盖物品（武器 7 / 盔甲 7 / 头盔 6 / 手套 6 / 靴子 6 / 饰品 17 / 材料 7 / 消耗品 10）、掉落表 9、技能数据 35、属性配置 5。
> 3. 这条管线的价值是**把策划和程序解耦**——策划改表不用等程序改代码。

---

## 第 4 部分：技术力综合评估

### 4.1 硬指标

| 指标 | 数值 | 评价 |
|------|------|------|
| 自研 C# 代码 | **25,983 行 / 274 个文件** | 实习岗中属于**明显偏上**。一般实习项目在 3k–8k 行 |
| 自研编辑器工具 | **2,121 行**（`Assets/Editor` 1118 + `Assets/Scripts/Editor` 1003） | **强项**。多数实习项目没有自研编辑器工具 |
| ScriptableObject 数据资产 | **114 个**（物品 66：武器 7 / 头盔 6 / 盔甲 7 / 靴子 6 / 手套 6 / 饰品 17 / 材料 7 / 消耗品 10；掉落表 9；技能数据 35；属性配置 5） | 数据驱动落地充分 |
| 内容规模（CSV） | `EquipmentAffixes.csv` **114 条词缀**、`Items.csv` 66 行、`Equipment.csv` 50 行、`LootTables.csv` 53 行、`CraftingRecipes.csv` 49 条配方 | 内容体量真实，不是空壳 |
| 状态机数量 | **3 套**（玩家 14 态 / 敌人 6 态 / Boss 4 阶段） | 强 |
| 敌人词缀 | **16 个**（4 数值类 + 12 行为类），按 5 类别加权抽取 | 强 |
| 接口抽象 | `IDamgable`、`ICounterable`、`IEnemyAffix`、`ILootable`、`IItemDropTarget`、`ISkillDropTarget`、`IHitStopable` | **强项**。接口驱动是良好工程习惯 |
| 第三方依赖 | DOTween、Cinemachine、TextMeshPro、LiteNetLib、Unity Input System | 使用克制，核心系统全部自研 |
| 开发周期 | 2026-07-17 → 2026-09-07（约 7.5 周），92 commits | 有效迭代速度 |
| 引擎版本 | Unity 6000.4.8f1 + URP + Cinemachine 2.10.5 + Input System 1.11.2 | 使用 Unity 6（较新版本） |
| ⚠️ 工程规范缺口 | **无 `.asmdef`、零单元测试** | 面试若被问"你写过测试吗"，要主动说这两项是缺口，并说明纯静态类（`RarityCalculator`/`AffixSelector`/`CraftingSystem`）天然可测却未测 |

### 4.2 七个技术亮点（按面试含金量排序）

| # | 亮点 | 为什么值钱 |
|---|------|-----------|
| 1 | **自研 GraphView 可视化编辑器** | 从"写代码的"升级为"能造工具的"。实习岗稀缺 |
| 2 | **技能间的组合式调用**（领域→回响/碎片） | 证明能设计可组合系统，而不只是堆功能 |
| 3 | **反击→追击的多子系统交接** | 涉及 4 个子系统的状态交接与冲突处理，体现并发/时序思维 |
| 4 | **Modifier + `source` key 的增量属性系统** | 装备/词缀/Buff 全部可叠加、可精准回退，且与添加顺序无关。这是"数值系统能扩展"的根本 |
| 5 | **双轨三段式顿帧** | 手感工程的具体知识，且实现有深度（逐目标字典、unscaled 计时、状态保存还原） |
| 6 | **Boss 的跨阈值边界处理** | 主动想到"一击跨两条阈值"这种反直觉边界，是成熟的 bug 预防意识 |
| 7 | **一张配方表双向服务制作与分解 + 概率透明化 UI** | 工程化意识（单一真源）+ 数值设计意识（可解释的随机）并存 |

**另外三个"小但很有味道"的实现细节**（面试当彩蛋讲，效果很好）：

| 细节 | 位置 | 为什么它聪明 |
|------|------|-------------|
| **自爆词缀用独立对象解耦生命周期** | `SelfDestructAffix` | 敌人死亡后 `Destroy(gameObject, 2)` 会连带销毁协程。它新建一个独立的 `SelfDestructBomb` GameObject 承载爆炸协程，从而不被连带销毁、也不被 `OnRemoved` 停掉。**这是对 Unity 生命周期陷阱的正确处理** |
| **用 100% 减速伪造 0.5 秒定身** | `LightningConduitAffix` | 玩家不是 `Enemy`，没有 `TryStun()`。它用 `playerEntity.SlowDownEntity(0.5f, 1f)` 把玩家速度降到 0 来模拟定身，并把理由写进了注释 |
| **"回血式"护盾吸收** | `BarrierShieldAffix` | `Entity_Health` 不认识"护盾"概念。它在 `OnTookDamage` 回调里把被护盾吸收的部分用 `IncreaseHP(absorbed)` **补回血量**来等效减免，并在注释里写明了局限（致命伤无法被护盾挡下，因为 `IncreaseHP` 不会复活死者）。**这种"用现有机制拼出新机制"的能力，比新写一个护盾系统更能体现工程判断** |

### 4.3 已知短板（面试被问到，用这个话术）

> 我在 2026-08-14 对项目做过一次**全系统并行代码审查**，产出了 12 份系统报告 + 1 份汇总（`production/review/`）。我自己列出的问题包括：

| 类别 | 具体问题 | 我的改进方案 |
|------|---------|-------------|
| **性能** | ① `Stat.GetValue()` 每次全量重算，无 dirty flag 缓存，且位于伤害计算热路径；② `OverlapCircleAll` 每次攻击分配数组（最多 3 次/击）；③ `FindAnyObjectByType` 出现在逐帧热路径（UI 槽拖放、Boss 死亡轮询）；④ **零对象池**——残影间隔最低 `0.04s`，一次 1 秒冲刺最多生成 25 个带 `SpriteRenderer` 的对象，全部 `Instantiate`+`Destroy`；⑤ `Entity_Stats.GetElementalDamage()` 每次调用 `new Dictionary` + `OrderByDescending` LINQ，在伤害热路径上；⑥ **AOE 暴击被独立随机 N 次**——`SkillObject_Base.DamageEnemiesInRadius` 在 `foreach` 循环体内 `new AttackData`，打 5 个敌人分配 10 次 | ① Stat 加 `isDirty` 缓存失效；② 改 `OverlapCircleNonAlloc` 复用缓冲；③ 改事件驱动 + 引用缓存；④ Unity `ObjectPool<T>`；⑤ 用三个 `float` 比较替代字典+LINQ；⑥ `GetAttackData` 提到循环外，AOE 共享一次暴击判定 |
| **架构一致性** | ① `Player_SkillManager.GetSkillByType` 是 12 分支硬编码 `switch` + `Awake` 里 12 行手工 `GetComponentInChildren`（**行为多态，注册/派发不是多态**）；② `Skill_TimeEcho` 用 `upgradeType == X` 等值判断导致 7 个分支只有一个生效，而 `Skill_Shard` 用 `if (Unlocked(...))` 允许共存——**同一套架构两套互斥语义**；③ `Skill_Base.SetSkillLevelData` 无条件 `totalLevel++`，升级和数据刷新两种场景都调用它，读档/重复同步会重复累加 | ① `GetComponentsInChildren<Skill_Base>()` 一次抓取，按 `skillType` 建 `Dictionary` 自动注册；② 分支判定改成 `upgradeTypeLevels[type] > 0`；③ `totalLevel` 由 `upgradeTypeLevels.Values.Sum()` 派生，消除双份真源 |
| **健壮性** | ① `StateMachine.ChangeState()` 无 null 检查、无同状态保护（重复 Exit+Enter 会重置计时器）；② `attack_PlayerVelocity` 数组无长度校验，Inspector 配短了会越界；③ **`ThickHideAffix`（血量加强）只改 maxHP 不重设当前血量**——词缀在 `InitializeHealth()` 之后注入，带该词缀的精英出生血条只有最大值的 77%（1/1.3），不是满血；④ **`ReactiveArmorAffix` 层键碰撞**——新增层用 `stackTimers.Count` 当后缀，而移除用 `i+1`，一旦"层到期后立刻再叠新层"就会撞键，导致 `RemoveModifier` 一次删掉两层、护甲数值泄漏；⑤ `Inventory_Item(LootedItem)` 构造**漏拷 `isPercentage`**，底材百分比属性会退化成固定值；⑥ `ShopSystem.ResetQuantity` 的 `Mathf.Clamp(1, 0, max)` **参数顺序写反** | ① 加 null 与同状态早退；② `OnValidate` 校验数组长度；③ 词缀注入后重设当前血量；④ 用独立递增计数器或列表下标做 key；⑤ 补齐字段拷贝；⑥ 修正为 `Mathf.Clamp(value, 1, max)` |
| **经济/数值** | **分解经济套利循环**：分解产出 = `round(配方材料数 × 0.5 × 稀有度倍率)`，而合成成本不随稀有度同步放大 → 木剑（3 铜）稀有打平、史诗产 4 铜、传说产 5 铜，**净赚** | 分解产出对稀有度做次线性映射（如 `sqrt`），或改为稀有度无关的固定回收值 |
| **性能/数据规模** | 背包用 `Dictionary<int, Inventory_Item>` 但 `FindItem`/`FindStackable`/`GetItemSlot`/`GetFirstAvailableSlot` **全是 O(n) 线性扫描**，缺 `itemData→slot` 反向索引与空闲槽队列；`Inventory_Base.itemList` 每次访问新建 List 且全工程无调用点（死代码 + GC 陷阱）；`CombineSystem.GetEquipmentPool` 每次合成都 `Resources.LoadAll` | 建反向索引字典 + 空闲槽队列；删除死属性；底材池启动时缓存 |
| **耦合** | `Player_Combat`（396 行）一个类管反击/追击/无敌/顿帧 4 套状态；`Player_Combat` 反向读 `basicAttackState.ComboIndex`；技能间横向硬耦合（`Skill_DomainExpansion.CastSpell` 直接调 `skillManager.timeEcho`/`shard`）；`EquipmentSystem` 同一份装备状态存在 3 个地方（两个字典 + 镜像 List）手工同步 | 拆出 `Player_CounterSystem`；攻击索引改为状态→战斗单向传递；技能间通过事件或接口解耦；`EquipmentSystem` 收敛为单一真源 |
| **资源生命周期** | 技能对象 Prefab 名称拼写错误且散落（`SkilllObjoct_DomainExpansion.prefab`、`Skill0bject_DomainExpansion.cs` 是数字 0 代字母 O）；`itemID` 只有 4 位十六进制随机后缀（65536 空间，同名同稀有度装备存在碰撞概率，会让 `RemoveModifier(source)` 误删另一件装备的加成） | 统一命名；`itemID` 改用完整 GUID 或自增实例序号 |
| **数值/文档一致性** | `LootDropItem.DropChance` 字段标注"掉落概率(0-100)"，**实际被当作相对权重用**（`CanDrop()` 是死代码）；合成注释写"同底材"而代码只要求同大类；`RarityCalculator.RarityMultipliers` 注释把 index 2 写成"优秀"而实际是"稀有"；`CombineSystem` 与 `EquipmentAffixGenerator` 各有一份**语义不同的** `GetMaxAffixCount` | 改名为 `DropWeight` 或实现真实概率语义；统一注释与实现；合并双份方法 |
| **死代码（8 处以上）** | `AffixSystemGenerator.cs` 无 `[MenuItem]` 完全不可达；`DismantleTable`/`DismantleEntry`/`DismantleMaterial` 零引用；`CombineSystem.GetBreakthroughPerQuality` 零调用；`LootDropItem.CanDrop`；`Inventory_Base.itemList`/`UpdateItemList`；`Player_WallJumpState` 实例化了但无任何转换进入；两个 Editor 里各一个赋值后从不使用的 `SerializedProperty` | 批量清理；`WallJumpState` 二选一（还原设计意图或删除） |
| **工程规范** | **无任何 `.asmdef`**（全部挤在 Assembly-CSharp，改一行全量重编译）；**零单元测试**（`CraftingSystem`/`CombineSystem`/`RarityCalculator`/`AffixSelector` 都是纯静态、天然可测却完全没测）；`ItemCSVImporter` 逐行 `AssetDatabase.FindAssets` 全库扫描（67 行 = 67 次全扫）；CSV 导入器对 SO 集做**破坏性删除无 dry-run**；`EquipmentAffixCSVImporter`/`CraftingRecipeCSVImporter` 每次导入 `DeleteAsset`+`CreateAsset` 重建整个 DB，导致外部引用失效与 GUID 抖动 | 按模块加 asmdef；为纯静态类补单元测试；导入器先建一次字典；破坏性操作加确认/预览 |
| **输入** | 技能槽用裸 `Input.GetKeyDown`（`SkillSlotManager.cs:99`）绕过 UI 阻塞链——UI 面板打开时技能仍可释放，聊天打字时 H/Y/U/I/O 误触发技能；O 键双绑定（`SkillSlot5` 与 `DomainExpansion`） | 改走 `GameInput.GetKeyDown(Action.SkillSlot1..5)`；`SetBinding` 加同键冲突检测 |
| **编码** | 约 10 个 `.cs` 文件为 GBK 编码、13+ 文件中文注释已乱码（含 `Entity.cs`、`StateMachine.cs`、`EntityState.cs` 等核心文件） | 统一 UTF-8（BOM）+ `.editorconfig` 强制；从 git 历史还原注释 |
| **跨平台/自动化** | `sync_xlsx_to_csv.ps1` 依赖 Windows + 本机安装 Excel COM + `-ExecutionPolicy Bypass`，CI 上跑不了；从编辑器起外部进程读 stdio 有挂起风险 | 改用 EPPlus/ClosedXML 之类的纯托管 Excel 库 |
| **未实现** | 能力门控（AbilityGate）、传送门网络（PortalManager）、任务触发分发层、`WorldState` 消费方、网络接入玩法 | 已列入迭代计划；属于规划缺口不是缺陷 |
| **文档漂移** | architecture.md 写"16 状态 FSM"，实际 14；GDD 记录的若干 bug 已修复但文档未回写 | 文档回写 |

> **关键话术**："这些问题是我自己审出来的，不是别人指出来的。我列出它们的价值在于——我知道一个系统写完不等于写好，性能、健壮性、耦合度都要主动去查。"

这句话会显著改变面试官对你的评级。

### 4.4 一段式的技术自述（可直接背，约 90 秒）

> "《破碎之城》是一个 2D 类银河恶魔城 + RPG 的 Demo，我独立完成了全部程序，约 26,000 行自研代码、274 个文件，另有 2,100 行编辑器工具，引擎是 Unity 6 + URP。
>
> 架构上，核心是三套组件式有限状态机：玩家 14 个状态、敌人 6 个状态、Boss 4 个阶段。状态是纯 C# 类不是 MonoBehaviour，通过构造函数注入宿主依赖，避免 `GetComponent` 开销。全局打断输入（冲刺、反击、领域展开）放在状态基类的 `Update` 里只写一次，不可用的状态用黑名单排除，这样新增状态默认就支持这些操作。
>
> 让我最有收获的是**反击到追击的连段**。它涉及反击窗口、眩晕、无敌帧、追击窗口四个子系统的交接。我踩过三个坑：顿帧延迟一帧执行，导致这一帧里敌人还能被二次反击；顿帧的 `FreezeAll` 会把追击突进的速度清零；反击和追击都管无敌帧会互相打架。解决方案是立即关闭敌人窗口、追击期间显式禁用顿帧并每帧清残留、追击接管时先清零反击的无敌计时器。
>
> 手感方面我做了土狼时间、五路独立输入缓冲、动态重力（上升 0.9 倍下落 1.3 倍），以及双轨三段式顿帧——全局顿帧改 `timeScale`，局部顿帧逐目标用 `AnimationCurve` 从静止抬升到原速，只冻结攻击者和受击者。这里必须用 `unscaledDeltaTime` 计时，否则 `timeScale = 0` 时计时器不会走。
>
> 技能系统里我最满意的是**组合式设计**：领域展开的回响分支和碎片分支不重新实现残影和碎片，而是通过技能管理器拿到已经存在的技能组件，直接调它们的生成方法。这样以后给残影加新分支，领域召唤出来的会自动继承。
>
> 工程化方面我写了两个工具：一个是基于 Unity GraphView 的任务树可视化编辑器，策划可以在画布上连任务链；另一个是 xlsx → CSV → ScriptableObject 的数据导入管线，带中文表头映射层，目前 114 个数据资产。
>
> 最后，我在八月做过一次全系统的并行代码审查，产出 12 份报告。我自己列出了性能热点、状态机健壮性问题、以及能力门控和传送门网络这些未实现的规划缺口——我的判断是，知道自己项目的边界和弱点，比声称它完美更重要。"

---

## 第 5 部分：演示前检查清单

录制前逐项确认：

### 🔴 必做（不做会直接影响演示效果）

- [ ] **补 4–6 个任务资产并连成一条前置链**（当前全库只有 1 个 `QuestData`，任务树编辑器画板上只有一个孤立节点，最强卖点无法展示）。用任务树编辑器操作即可，纯编辑器工作，不写代码
- [ ] **不要展示**：独立能力门组件 `AbilityGate`、传送门网络、网络联机、任务自动/拾取触发、`WorldState` 改变世界
- [ ] **要展示**：技能树门控（点亮二段跳前后过同一段地形）——这是门控的真实实现，说法见 A 档说明
- [ ] **要展示**：存档往返（限定范围：装备词缀/技能/任务还原 + JSON 可读性），**不展示** Boss/传送门状态
- [ ] **不要现场演示"存档→读档"**（Boss/传送门状态不持久化）。如要展示，只展示**背包与装备词缀读档后完全一致**（这一条是好的）
- [ ] **关掉 `Debug.Log` 刷屏**——`Player_Combat` / `PlayerState` / `SkillSlotManager` / `Player_SkillManager` / `Boss_SlimeKing` 都有高频日志，Console 会被刷爆，录制时画面会持续跳动
- [ ] Console 清空后完整跑一遍，确认无报错弹窗
- [ ] 准备好架构分层图（用 `docs/architecture/architecture.md` 里的分层图）

### 🟡 玩法验证（每条都要能稳定复现）

- [ ] Boss 战能一次打通完整阶段转换（含 30% 分裂 + 濒死隐身保命）
- [ ] **反击→追击连段稳定复现**（这条最容易手抖，多练几次）
- [ ] 领域展开三个分支都能释放（减速 / 回响 / 碎片）
- [ ] 元素状态三种都能触发（火 DoT / 冰减速 / 雷落雷）
  - ⚠️ **别讲"冰抗减少减速时间"**——`Entity_StatusHandler.cs:91` 的 `finalDuration` 算了但没传进协程，是死代码，冰抗时长减免实际未生效
- [ ] 时间回响的复制分支能从克隆体打出第二个回响
- [ ] 碎片传送（两次施法交换位置）能稳定触发
- [ ] 技能树能开、能解锁、能洗点、能关
- [ ] **任务树编辑器能打开、能拖节点连线、能保存**（需要先完成上面的"补任务资产"）
- [ ] CSV 导入窗口能打开（展示界面即可，不必真跑导入——**导入器有破坏性删除且无 dry-run**）
- [ ] 铁匠三页签（制作/分解/合成）都能开，合成面板的概率逐行显示正常
- [ ] 精英词缀能稳定刷出并触发（火焰光环 / 冰霜新星 / 镜像分身 / 自爆 / 屏障护盾）
  - ⚠️ 如果刷到带**"血量加强"（ThickHide）**的精英，它的血条只有最大值 ~77% 不是满血——**这是已知 bug**，录制时避开或别停留

### 🟢 录制设置

- [ ] 1080p 60fps（顿帧 0.08s 在 30fps 下只有 2–3 帧，看不出效果）
- [ ] 顿帧和反击镜头单独慢放录制
- [ ] 玩法镜头先录（容易被打断），编辑器镜头后补（一次就过）
- [ ] 掉落演示时**不要掉大额货币**——`ApplyDropPhysics` 的水平初速对序号无上限，大额金币会飞出屏幕（这是已知 bug，别无意中录进去）

---

## 附：一页速查卡（面试前 5 分钟看这个）

**报数字**：25,983 行 / 274 文件 / 2,121 行编辑器工具 / 114 个 SO / 66 件物品 / 114 条词缀 / 49 条配方 / Unity 6 + URP / 7.5 周 92 commits

**三套 FSM**：玩家 14 态 · 敌人 6 态 · Boss 4 阶段

**七个必讲亮点**：
1. 自研 GraphView 任务树编辑器
2. 技能组合式调用（领域→回响/碎片）
3. 反击→追击 的 4 子系统交接（三个踩坑）
4. Modifier + `source` key 的增量属性系统（可叠加、可精准回退、与顺序无关）
5. 双轨三段式顿帧（逐目标字典 + unscaled 计时 + 状态保存还原）
6. Boss 跨阈值边界处理（一击跨分裂线+致死线）
7. 一张配方表双向服务制作与分解 + 概率透明化 UI

**三个彩蛋细节**（讲出来很加分）：
- 自爆词缀新建独立对象承载协程，避开 `Destroy(gameObject, 2)` 的连带销毁
- 用 `SlowDownEntity(0.5f, 1f)` 把玩家速度降到 0 来伪造 0.5 秒定身
- 护盾用 `IncreaseHP(absorbed)` 回补血量实现等效伤害吸收

**四个必答的技术细节**：
- 土狼时间 0.1s / 五路独立输入缓冲（跳跃分地面/空中两条通道，消费后必须 Clear）
- 动态重力：上升 0.9×、下落 1.3×、二段跳下落 1.6×
- 护甲 `armor/(armor+200)` 上限 60%，抗性上限 75%，闪避上限 75%（闪避先于无敌帧判定）
- Stat 双乘区：`(base + Σ固定值) × (1 + Σ百分比)`，`source` key 精准回退

**一个可现场心算的数值例**：普通底材 + 无词缀普通怪 → P(升精良) ≈ 34.6%；同一底材 + 3 词缀传说精英（+60% 掉落加成）→ ≈ 49.2%

**六个主动认领的短板（挑 3–4 个说，别一次全倒）**：
1. `Stat.GetValue()` 无缓存，每次全量重算，且在伤害热路径上
2. 零对象池——残影 0.04s 一个，一次冲刺最多 25 个对象 Instantiate/Destroy
3. `GetSkillByType` 是硬编码 switch（行为多态，注册派发没多态）
4. `ThickHideAffix` 只改 maxHP 不重设当前血量 → 精英血条只有 ~77%
5. 分解经济存在套利循环（高稀有度分解产出 > 制作成本）
6. 无 `.asmdef`、零单元测试

**四个不要提的**：独立能力门组件 `AbilityGate` · 传送门网络 · 网络接入玩法 · 任务自动/拾取触发 + `WorldState` 改变世界 + **Unity Input System / Action Maps**（代码里是旧版 `Input.GetKeyDown`，全项目零处 `using UnityEngine.InputSystem`）

**三个"面试官可能追问"的标准答案**：
- *"你项目里有什么性能问题？"* → Stat 无缓存 + 零对象池（技能对象/残影/伤害数字/刷怪全是 Instantiate+Destroy）+ 循环内 `new AttackData`（AOE 暴击还被打散成 N 次独立判定）+ 热路径 LINQ/字典分配 + 多处 `FindAnyObjectByType` 在 Update 里
- *"你的代码有测试吗？"* → 没有，这是我最大的工程规范缺口；但 `RarityCalculator`/`AffixSelector`/`CraftingSystem`/`ShopSystem` 都是纯静态或纯业务类，是天然的单元测试对象
- *"如果重做你会改什么？"* → ① 属性系统加 dirty flag；② 技能注册改 `GetComponentsInChildren` 字典自动发现；③ 把"单例 + `FindAnyObjectByType`"收敛成显式注入；④ 补 asmdef、单元测试和原子化存档写入

**最后一条最重要的策略**：**主动暴露短板**。你手上有 `production/review/` 里 12 份自己做的系统审查报告——"我审过自己的项目，列出 40+ 条问题并排了修复优先级"这件事本身，比任何单个功能都更能说明工程成熟度。
